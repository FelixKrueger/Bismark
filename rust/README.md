# Bismark Rust rewrite

Active rewrite of Bismark from Perl to Rust. Progress is tracked at
the [Bismark Rust rewrite project](https://github.com/users/FelixKrueger/projects/1).

Working branch: [`rust/iron-chancellor`](https://github.com/FelixKrueger/Bismark/tree/rust/iron-chancellor).

## Layout

- `bismark-io/` — shared library: BAM/SAM/CRAM I/O via [noodles](https://github.com/zaeleus/noodles). See `bismark-io/DESIGN.md` for the design contract.
- Per-binary crates are added incrementally (`bismark-dedup/`, `bismark-bedgraph/`, `bismark-extractor/`, …). Phase 1 priorities are tracked on the project board.

## Binary naming during coexistence

Rust binaries take an `_rs` suffix through approximately v0.26 → v1.0 so they can be installed alongside the Perl Bismark scripts on the same PATH without conflicts:

| Perl                            | Rust binary (during coexistence) |
|---------------------------------|----------------------------------|
| `deduplicate_bismark`           | `deduplicate_bismark_rs`         |
| `bismark_methylation_extractor` | `bismark_methylation_extractor_rs` |
| `bismark2bedGraph`              | `bismark2bedGraph_rs`            |
| `coverage2cytosine`             | `coverage2cytosine_rs`           |

After v1.0 of the Rust port, the `_rs` suffix is dropped — the Rust binaries become the default `deduplicate_bismark` etc., and the Perl scripts move to a `legacy/` directory.

**Inside the published container image** the tools are *additionally* exposed under their **canonical names** (`bismark`, `deduplicate_bismark`, `coverage2cytosine`, …): a container has no Perl Bismark to collide with, so canonical names make it a drop-in for pipelines that call the tools by name (e.g. nf-core/methylseq). The `bismark` canonical name is a thin wrapper that answers `-v`/`--version` with a `Bismark v0.25.1`-compatible banner (so the pipeline's version capture is byte-identical to the Perl oracle) and otherwise execs `bismark_rs`; the rest are symlinks. Host installs keep the `_rs` suffix for Perl coexistence.

## Architecture decisions

- **BAM/SAM/CRAM I/O via pure-Rust `noodles`** — no `rust-htslib` (no htslib C build-time dep), no `samtools` subprocess (no external runtime dep).
- **One cargo workspace** with a binary crate per Bismark tool plus the shared `bismark-io` library. Library+binary split per crate so pure logic is unit-testable.
- **Byte-equal output to Perl Bismark v0.25.1** is a CI gate for the tools we have validated.
- Edition 2024; MSRV pinned in the workspace manifest.

## Installing

<!-- Maintainer: on a suite-version bump, update the pinned `docker pull :<ver>` tag + the
     `cargo install bismark@<ver>` example here, `suite_tag` in `rust/justfile`, and the matching
     section in the docs site (`docs/src/content/docs/installation.md`). The `:latest` container tag
     and `cargo install bismark` (+ `--force`) track the newest release automatically. -->

Three ways to get the suite, easiest first — pick **one**. (A bioconda `mamba install bismark`
giving the Rust suite is coming as a fast-follow; until then `bismark` on bioconda is the Perl v0.25.x.)

### 1. Prebuilt binaries (no Rust toolchain needed)

Each [release](https://github.com/FelixKrueger/Bismark/releases) attaches prebuilt binaries for common Linux/macOS platforms. Download the archive for your platform, extract it, and put the binaries on your `PATH`. The tools use their canonical names (`bismark`, `deduplicate_bismark`, …).

### 2. Container image (nothing to install)

A multi-arch image is published to the GitHub Container Registry:

```bash
docker pull ghcr.io/felixkrueger/bismark:latest   # latest release
docker pull ghcr.io/felixkrueger/bismark:2.0.0     # pinned
```

The tools use their **canonical** names (`bismark`, `deduplicate_bismark`, …), so the image is a drop-in for pipelines such as nf-core/methylseq. (The `bismark` command in the container is a thin version-probe wrapper that reports the suite version in methylseq's expected format, then execs the real aligner; every other tool is the real binary.)

> **nf-core/methylseq drop-in — one known limitation + one behavior note** (the CLI surface is
> otherwise conformance-tested against methylseq 4.2.0; see each crate's `tests/methylseq_conformance.rs`):
> - **`--local` alignment** (`params.local_alignment` / `--local`) is supported for **Bowtie 2 and
>   HISAT2** (byte-identical to Perl `--local`; HISAT2-local drops `--no-softclip` + uses the L-form
>   `--score-min L,0,-0.2` + the local `ln()` MAPQ, faithful to Perl). **minimap2-`--local` is rejected
>   — minimap2 performs local (soft-clipping) alignment _by design_** (no end-to-end vs local distinction
>   to toggle); `--local` with `--combined_index` is also rejected (fail-loud).
> - **HISAT2 (`--aligner bismark_hisat`) + `--multicore N`** runs as a **single HISAT2 instance with
>   `-p N` threading** (`--reorder`), byte-identical to Perl `--hisat2 -p N`. ⚠️ This is a deliberate
>   remap (`--multicore`→`-p`, **HISAT2 only**): the fork+chunk model is *not* faithful for HISAT2
>   (splice-site discovery is not chunk-invariant), so the result is deterministic per N but
>   **depends on the thread count** — it is NOT identical to single-core HISAT2 (HISAT2's nature; Perl
>   has the same behaviour). The run prints a never-silent notice. methylseq's auto-derived
>   `--multicore = cpus/3` now works directly — **no cpus-cap workaround needed.** Bowtie 2
>   `--multicore` (the worker-invariant fork path) and single-core HISAT2 are unaffected.

### 3. Build from source with `cargo install` (whole suite, one command)

Requires a Rust toolchain (see Prerequisites below). The `bismark` crate is a
batteries-included installer — one command builds **all 12** binaries into
`~/.cargo/bin`:

```bash
cargo install bismark
```

To install a single tool instead, install its crate, e.g. `cargo install bismark-aligner`
(others: `bismark-dedup`, `bismark-extractor`, `bismark-bedgraph`,
`bismark-coverage2cytosine`, `bismark-genome-preparation`, `bismark-bam2nuc`,
`bismark-nome-filtering`, `bismark-filter-nonconversion`,
`bismark-methylation-consistency`, `bismark-report`, `bismark-summary`).

> **Updating.** `cargo install bismark --force` reinstalls the newest published
> version in place. Pin a specific release with `cargo install bismark@2.0.0`.

Compiling the suite from source is a non-trivial one-time build.

#### Prerequisites (cargo path)

- **Rust** — latest stable recommended (`rustup update`). The workspace MSRV is **1.89**; the one-command install above was verified on **cargo 1.95** (older cargo may not resolve packages inside the `rust/` subdirectory).
- A working **C linker** (`cc`) for a few transitive build dependencies.
- **Alignment backends on `PATH`** — only the aligner and genome-preparation tools shell out to an external program: **Bowtie 2** + `bowtie2-build` (default), or optionally **HISAT2** + `hisat2-build`, or **minimap2**. `cargo install` builds the Rust tools, not these backends. *(No `samtools` is required — BAM/SAM I/O is pure-Rust `noodles`.)*
- Ensure **`~/.cargo/bin` is on your `PATH`** to run the installed binaries.

### Legacy: the Perl Bismark (v0.25.1)

The final Perl release is **v0.25.1** — the byte-identity target the Rust suite reproduces. To install the Perl Bismark independently of the Rust suite:

```bash
conda install -c bioconda bismark=0.25.1            # bioconda
```

Or run it from source: download the [v0.25.1 release](https://github.com/FelixKrueger/Bismark/releases/tag/v0.25.1) (or `git checkout v0.25.1`) — the Perl scripts (`bismark`, `deduplicate_bismark`, `bismark_methylation_extractor`, …) live at the repository root and need **Perl** + a **Bowtie 2 / HISAT2** backend + **samtools** on `PATH`.

## Combined-index alignment (v2, opt-in — beta)

`bismark_rs` can align against a **single combined CT+GA index** instead of the faithful 2 (directional) / 4
(non-directional) separate per-strand aligner instances. It is **opt-in, never-silent, and concordance-gated —
NOT byte-identical** to the faithful default (a small benign churn vs the per-strand oracle: directional ~0.013 %,
non-directional ~0.022–0.044 %, pbat ~0.044 %, almost all unique↔ambiguous flips). The faithful default path is
unchanged; use combined mode only when you opt in.

**Prerequisite — build the combined index once** (genome-prep adds `Bisulfite_Genome/Combined/`):

```bash
bismark_genome_preparation_rs --combined_genome /path/to/genome/   # add --hisat2 for a HISAT2 combined index
```

**Coverage:** single-end **and** paired-end · **Bowtie 2 and HISAT2** · directional / non-directional / pbat — all
concordance-gated against the faithful per-strand path. (minimap2-combined and `--multicore` + combined are **not**
supported and fail loud.)

**The flags:**

| Flag | What it does | Scope |
|------|--------------|-------|
| `--combined_index` | One both-strands pass per read-conversion over the combined index. Non-directional uses the default **parallel** model (a) (two concurrent passes). | SE+PE · Bowtie 2+HISAT2 · dir/non-dir/pbat |
| `--combined_index_sequential` | **Faithful** low-RAM variant for non-directional: run the two both-strands passes **one at a time** (pass 1 spills + its aligner exits, freeing the index, before pass 2 starts) — one index resident → ~½ the peak RSS. **Byte-identical** to model (a). | non-dir only · SE+PE · Bowtie 2+HISAT2 |
| `--combined_index_single_pass` | Model (b): ONE pass over conversion-tagged interleaved reads (one index load). **NOT byte-identical / NOT decision-equivalent** to model (a) (the qname tag perturbs Bowtie 2's read-name RNG) — opt-in, ground-truth-validated, never the default. | non-dir only · SE+PE · **Bowtie 2 only** |

```bash
# directional, combined index, one both-strands pass (16 threads):
bismark_rs --combined_index --genome /path/to/genome/ -p 16 reads.fq.gz

# non-directional, faithful low-RAM (one index resident at a time):
bismark_rs --combined_index --non_directional --combined_index_sequential --genome /path/to/genome/ -p 16 reads.fq.gz
```

**Memory characteristics** (real 10M WGBS SE, GRCh38, 16-core budget — see
`plans/06092026_bismark-beta/BENCHMARK_RESULTS_alignment_modes.md`): the combined index is one *large* index
(~9.5 GB resident), so the **RAM win is the low-RAM non-directional variants**, not combined-parallel: non-dir
faithful **16.3 GB** / 4 small indices · combined parallel (a) **19.0 GB** / 2 large · combined
**sequential / single-pass 11.1 GB / 1 large** (−32 % vs faithful, −41 % vs parallel (a)). Combined directional /
pbat are one index load (fastest walls) but ~11 GB ≈/> the faithful 2-small-index layout, so they are not a RAM win.

## Building

```bash
cd rust
cargo build --release
```

## Status

One headline per module — current state at a glance. Per-crate detail lives in each crate's `README.md` / `CHANGELOG.md`; the dated shipping log is under [Milestones](#milestones). Rows are in rough pipeline order. State key: ✅ shipped on `rust/iron-chancellor` · 🚧 in progress · ⬜ planned.

| Perl tool | Rust crate (binary) | Version | State |
|---|---|---|---|
| _(shared library)_ | `bismark-io` | 1.0.0-beta.9 | ✅ noodles BAM/SAM/CRAM I/O + `ThreadedBam{Reader,Writer}` (parallel BGZF); byte-equal output is a CI invariant for consumers |
| `bismark_genome_preparation` | `bismark-genome-preparation` (`bismark_genome_preparation_rs`) | 1.0.0-beta.1 | ✅ Converted CT/GA FASTA **byte-identical** to Perl v0.25.1 + `--genomic_composition`; all 3 aligners (Bowtie2 / HISAT2 / minimap2), indexing delegated to the external indexer |
| `bismark` (aligner) | `bismark-aligner` (`bismark_rs`) | 1.0.0-alpha.1 | ✅ **All 10 phases complete** (#930 = Ph 1–8, #942 = Ph 9a, #945 = Ph 9b; **Ph 10 full-scale real-data gate PASSED**, #948). Bowtie 2 backend, **SE + PE, FastQ + FastA, all 3 library types (directional / non-directional / pbat)** — read-conversion → 2–4 instances → lockstep merge/scoring/MAPQ → `XM`/`XR`/`XG` → BAM + report + `--unmapped`/`--ambiguous`/`--ambig_bam`, **byte-identical** to Perl v0.25.1 + Bowtie 2 2.5.5 at 1M reads/pairs and **content byte-identical at full real-data scale** (Ph 10 on oxy: 84M SE / 84M PE / 46.7M mouse-RRBS GRCm39 / pbat; 173/181/52 contigs; + V13 cross-check vs the pre-existing Perl `--parallel 4` BAMs). **Order-preserving `--multicore`/`--parallel`** (worker-count-invariant). The ~74% runtime "big beast" — **faithful (Bowtie 2) port complete**. **v1.x backend set COMPLETE** — **HISAT2** (SE+PE, FastQ+FastA, all 3 libraries, byte-identical to Perl v0.25.1 + HISAT2 2.2.2; `--hisat2 --multicore N` = single instance `-p N` threading, byte-identical to Perl `--hisat2 -p N` — fork model not faithful, splice discovery is input-batch-global; #949) **and minimap2 SE** (byte-identical to Perl v0.25.1 + minimap2 2.31-r1302, clean-slate `-x map-ont` options + positional `.mmi`; SE only — PE deferred, no trustworthy Perl oracle; worker-invariant; #950). **Phase-5 combined 10M gate: all 13 cells byte-identical** (Bowtie 2 + HISAT2 SE+PE + minimap2 SE × {dir, non-dir, pbat} + mouse **GRCm39** RRBS). **v2 experimental: `--rammap`** — the pure-Rust minimap2 reimplementation (`jwanglab/rammap`, subprocess via `--path_to_rammap`) as a 4th backend; opt-in, never-silent, **concordance-gated (NOT byte-identical to minimap2)**. Minimap-like: single `.mmi`, clean-slate `-x map-ont`, SE-only, same `--mm2_*` knobs (apples-to-apples vs `--minimap2`), `--local`/PE/`--combined_index` rejected. `--multicore N` allowed/worker-invariant (per-read-independent + deterministic). **In-process opt-in `--rammap_inprocess`** (feature `rammap-inprocess`, default-OFF, release-OFF): each `.mmi` loaded once + `Arc`-shared, per-read `map_seq` **parallelized** on a `--multicore`-sized shared rayon pool (#995) — **−54 % RSS AND ~1.8× faster than the subprocess** (oxy V6: 1M non-dir 1382 s vs subprocess 2451 s; 32.3 vs 70.9 GB; worker-invariant; 11.4× over serial), concordance-gated vs the subprocess (≤0.022 %/cell divergent, one-directional); still a compile-time opt-in (release-OFF) pending the release-flip follow-up. epic `plans/06052026_bismark-aligner-v1x/` (subprocess) + `plans/06152026_rammap-library-integration/` (in-process) + `plans/06192026_rammap-inprocess-parallel/` (#995 parallel). **Opt-in single-cell tags `--add_barcode` / `--add_umi`** — `CB:Z:`/`UR:Z:` parsed from the QNAME (SeekSoul `<barcode>_<umi>_…` format); default-off + byte-identical when off, both PE mates tagged, never-silent on malformed names; `plans/06242026_umi-barcode-tags/`. **Unaligned BAM (uBAM) input** (#1026 SE + #1027 PE) — accept a uBAM as read input alongside FastQ/FastA, auto-detected by BAM magic (not extension): reads are transcoded to a temp FASTQ matching `samtools fastq` and fed to the unchanged pipeline, so output is **byte-identical to the equivalent `samtools fastq` run** (oxy-gated SE + PE × dir/non-dir/pbat); a single name-collated PE uBAM passed positionally is auto-split into mates; `plans/06252026_ubam-input/`. **BINSEQ (`.vbq`) input** (#1025 Phase 2) — accept Arc Institute VBQ as read input (SE + PE), decoded **in-process** via the `binseq` crate to a temp FASTQ matching `bqtools decode`, so output is **identical to the equivalent `bqtools decode` run**; VBQ only (`.cbq`/`.bq` recognised but rejected fail-loud), per-read quality + headers required (D2 — a quality-less/name-less VBQ is rejected, never `?`-filled), one VBQ record carries both PE mates → auto-split. Feature-gated `binseq-input` (default-OFF; release-ON; a default build still rejects `.vbq` never-silently); the workspace `thiserror`/`anyhow` pins were bumped to satisfy `binseq` (byte-output-neutral, error text is stderr-only). `plans/06252026_pluggable-inputs/phase2-binseq-input/`. **Illumina 5-Base (5mC→T) support** (#1015 / #787) — opt-in `--illumina_5base` (paired-end only): the chemical inverse of bisulfite (the enzyme converts 5mC→T and leaves unmethylated C), so raw reads align to the **unconverted** genome (minimap2 `-x sr` default; bowtie2/hisat2 via `--five_base_index`) with inverted-polarity calls. **Concordance-gated, NOT byte-identical** (no Perl 5-Base oracle — the `--rammap`/`--combined_index` precedent): per-CpG **r ≈ 0.99 vs DRAGEN over 55M CpGs** on real NA12878. Experimental/preview modes (`--five_base_duplex` / `--five_base_consensus` / `--five_base_umi_qname` / `--five_base_deconvolution`) behind opt-in, never-silent flags, concordance-gated by a reproducible **lambda/pUC19 spike-in** CI gate; the faithful bisulfite/EM-seq suite stays **byte-frozen** (every legacy `methylation_call` passes `five_base=false` → bit-identical; perl-oracle green). `plans/06232026_illumina-5base-support/` |
| `deduplicate_bismark` | `bismark-dedup` (`deduplicate_bismark_rs`) | 1.2.1-beta.2 | ✅ **Byte-identical** to Perl v0.25.1 on real-data WGBS (10M + ~55M PE); UMI/RRBS modes; optional `--parallel N` BGZF threading; **non-directional PE** (CTOT/CTOB swapped-FLAG pairs) fixed (#1030) |
| `filter_non_conversion` | `bismark-filter-nonconversion` (`filter_non_conversion_rs`) | 1.0.0-beta.1 | ✅ **Byte-identical** to Perl v0.25.1 (9 golden cells + oxy 10M SE + PE × 4 decision modes) |
| `NOMe_filtering` | `bismark-nome-filtering` (`NOMe_filtering_rs`) | 1.0.0-beta.1 | ✅ **Byte-identical** to Perl v0.25.1 (synthetic goldens + full 10M SE oxy gate); **~3.4×** |
| `bismark_methylation_extractor` | `bismark-extractor` (`bismark_methylation_extractor_rs`) | 1.0.0-beta.2 | ✅ **Byte-identical** at full scale (WGBS PE/SE + RRBS, worker-count-invariant); **~4.8×** vs Perl `--multicore 12`. **Inline streaming**: `--bedGraph`/`--cytosine_report` drive bismark2bedGraph + coverage2cytosine **in-process** (in-memory tee, no Perl subprocesses) — byte-identical downstream incl. `--CX` (Phase H sub-gate 2). **v1.x**: multiple input files (looped per-file, no pooling) + single-end **coordinate-sorted** input accepted (PE coord-sorted still rejected); `--CX` valid with `--bedGraph` alone (all-context coverage/bedGraph, no `--cytosine_report` needed) — all faithful to Perl. **non-directional PE** (CTOT/CTOB swapped-FLAG pairs) fixed (#1030) |
| `bismark2bedGraph` | `bismark-bedgraph` (`bismark2bedGraph_rs`) | 1.0.0-beta.1 | ✅ **Byte-identical** (decompressed content); **~3.4×** |
| `coverage2cytosine` | `bismark-coverage2cytosine` (`coverage2cytosine_rs`) | 1.0.0-beta.2 | ✅ **Byte-identical** core + niche modes (`--gc`/`--nome-seq`/`--drach`/`--ffs`) vs Perl v0.25.1; 15-cell full-hg38 oxy gate; **~12×** CpG-report / **~2.6×** `--CX` |
| `bam2nuc` | `bismark-bam2nuc` (`bam2nuc_rs`) | 1.0.0-beta.1 | ✅ **Byte-identical** to Perl v0.25.1 (mono/di-nucleotide stats; local goldens + oxy real-data gate) |
| `methylation_consistency` | `bismark-methylation-consistency` (`methylation_consistency_rs`) | 1.0.0-beta.1 | ✅ **Byte-identical** output vs Perl v0.25.1 |
| `bismark2report` | `bismark-report` (`bismark2report_rs`) | 1.0.0-beta.1 | ✅ **Byte-identical** HTML vs Perl v0.25.1 (modulo the `localtime` timestamp line); synthetic + real WGBS PE (10M + ~55M) |
| `bismark2summary` | `bismark-summary` (`bismark2summary_rs`) | 1.0.0-beta.1 | ✅ **Byte-identical** project-level multi-sample summary (HTML + `.txt`) vs Perl v0.25.1 |

Versions are the crate manifests on `rust/iron-chancellor` (a release **git tag** such as `…beta.2` may lead its manifest version). "Byte-identical" = validated against Perl Bismark v0.25.1 per each crate's README/CHANGELOG + golden/real-data tests; speedups are full-scale where measured. `bismark-io` and `bismark-dedup` have early beta lines published to crates.io; later betas are queued for the next publish window.

> **Keeping this journal current:** every module-merge PR into `rust/iron-chancellor` should update that tool's row above **and** add a dated line to [Milestones](#milestones). The helper scripts (`copy_bismark_files_for_release.pl`, the `merge_*coverage*` Python utilities) are out of scope for the Rust port.

## Milestones

Reverse-chronological log of the main Rust-rewrite shipping events (merges into `rust/iron-chancellor`). One headline per event; per-crate detail is in the crate READMEs/CHANGELOGs.

- **2026-06-30**: `bismark` aligner **5-Base narrowed to paired-end only** (single-end scaffold removed); #787 follow-up.
- **2026-06-29** — `bismark` aligner **Illumina 5-Base (5mC→T) support** (#1015, #787; `plans/06232026_illumina-5base-support/`) — opt-in `--illumina_5base` (paired-end only): 5-Base is the chemical inverse of bisulfite (the enzyme converts 5mC→T and leaves unmethylated C), so raw reads align to the **UNCONVERTED** genome (minimap2 `-x sr` default; bowtie2/hisat2 via `--five_base_index`) and methylation is called at **inverted polarity**. **Concordance-gated, NOT byte-identical** — there is no Perl 5-Base oracle (the `--rammap`/`--combined_index` precedent): per-CpG **r ≈ 0.99 vs DRAGEN over 55M CpGs** on real NA12878 5-Base data (global CpG 49.7–50.1 % vs DRAGEN 49.98–50.48 %). Experimental/preview modes — `--five_base_duplex` / `--five_base_consensus` (both strands, DRAGEN-validated) / `--five_base_umi_qname` / `--five_base_deconvolution` (90.3 % precision / 93.4 % recall vs DRAGEN's VCF @40×) — ship behind opt-in, **never-silent** flags, labeled EXPERIMENTAL/PREVIEW, and are concordance-gated by a **reproducible lambda/pUC19 spike-in** CI gate (known truth, no proprietary data). The faithful bisulfite/EM-seq suite stays **byte-frozen**: every legacy `methylation_call` passes `five_base=false` → reduces to the exact original literals (bit-identical; `combined.rs` untouched, `bismark-io` additive), confirmed by the green `perl-oracle` byte-identity gate. Dual code-review **APPROVE-WITH-NITS** (non-blocking follow-ups #1035). Squash `78cb85a`.
- **2026-06-26** — **non-directional paired-end fix** (#1030) — `deduplicate_bismark_rs` and `bismark_methylation_extractor_rs` crashed on every `--non_directional` PE BAM (`read identity mismatch: expected R1 for first mate, got R2`). Bismark deliberately swaps the SAM R1/R2 FLAG bits for CTOT/CTOB pairs; the shared `bismark_io::BismarkPair::from_mates` gate rejected the swap while Perl never inspected those bits. Fixed in `bismark-io` (pairs by file order + qname only; no version bump — non-breaking). Byte-identical to Perl v0.25.1 on the reproducer for both tools (dedup BAM + extractor CpG/CHG/CHH/M-bias/report). `bismark-dedup` → `1.2.1-beta.2`, `bismark-extractor` → `1.0.0-beta.2`.
- **2026-06-26** — `bismark` aligner **BINSEQ (`.vbq`) read input** (#1025 Phase 2; plan `plans/06252026_pluggable-inputs/phase2-binseq-input/`, epic `plans/06252026_pluggable-inputs/`) — the 2nd backend of the pluggable-input front-end (after uBAM), plugged into the same `InputDecoder`/`resolve_alt_inputs` seam. Accept an [Arc Institute BINSEQ](https://github.com/arcinstitute/binseq) `.vbq` as read input (SE + PE), decoded **in-process** via the `binseq` crate (0.9.2) — the serial, file-order `vbq::MmapReader` (NOT the parallel `BinseqReader`, which would reorder + mis-pair) — to a temp FASTQ matching `bqtools decode` and fed to the **byte-frozen** convert→align→merge core, so output is **identical to the equivalent `bqtools decode` run**. One VBQ record carries both PE mates → auto-split (no collation). **VBQ only**: `.cbq`/`.bq` are recognised (extension) but **rejected fail-loud** (BQ has no quality/names; CBQ is a documented fast-follow — its serial reader isn't cleanly exposed by binseq 0.9.x). **D2**: a quality-less or name-less VBQ is rejected at the file-header level (the crate `?`-masks per-record absence, so per-record checks would silently pass) — never silently `?`-filled. Feature-gated `binseq-input` (default-OFF, **release-ON**, mirrors `rammap-inprocess`); detection is always-compiled so a default build rejects `.vbq` never-silently. The byte format was pinned from `bqtools` 0.5.7 source; the workspace `thiserror` (=2.0.0→=2.0.18, 12 crates) + `anyhow` (=1.0.86→=1.0.103, 4 crates) pins were bumped to satisfy `binseq` (patch-level, byte-output-neutral — error text is stderr-only). Dual code-review APPROVE + plan-manager COMPLETE; hermetic tests assert byte-exact SE/PE output + the rejects + multi-block + determinism. **Oxy equivalence gate PASSED 12/12 byte-identical in-order** (1M GRCh38 reads, Bowtie 2 2.5.5): `bismark_rs <.vbq>` == `bismark_rs <bqtools decode -T1>` for BINSEQ SE+PE × {dir, non-dir, pbat} (6 cells) AND the **uBAM R8 re-gate** `bismark_rs <ubam>` == `bismark_rs <samtools fastq>` SE+PE × 3 libs (6 cells — confirms the shared-seam refactor didn't regress uBAM). Gate detail: `plans/06252026_pluggable-inputs/phase2-binseq-input/GATE.md`.
- **2026-06-25** — `bismark` aligner **unaligned BAM (uBAM) read input** (#1025; plan `plans/06252026_ubam-input/`, epic `plans/06252026_pluggable-inputs/`) — accept a uBAM as read input in addition to FastQ/FastA. **Single-end** (#1026) + **paired-end** (#1027 — a single name-collated PE uBAM, passed positionally, auto-detected via the first primary record's `0x1` and split into mates). Auto-detected by **BAM magic bytes, not extension** (a FastQ / `.fq.gz` / SAM-text file is never mis-treated); reads are transcoded to a temp FASTQ matching `samtools fastq` (full-IUPAC reverse-complement on `0x10`, `B` on missing qual, secondary/supplementary skipped) and fed to the **byte-frozen** convert→align→merge core, so the output is **byte-identical to the equivalent `samtools fastq` run** — oxy real-data gate **3/3 byte-identical for SE AND PE × {directional, non-directional, pbat}** (GRCh38 + Bowtie 2 2.5.5). Additive + never-silent: uBAM + `--fasta`, a uBAM via `-1/-2`, mixing a PE uBAM with other inputs, and minimap2/rammap-PE all fail loud (the SE→PE switch re-runs the shared `reject_unsupported_paired_aligner` so the guard can't drift). Each merge: dual code-review APPROVE + plan-manager COMPLETE. First backend of the pluggable-input front-end (BINSEQ / mim / paraseq are later phases).
- **2026-06-27** — **`bismark-rust-v2.0.0-beta.13` released** — Rust suite beta carrying everything merged since `…beta.12`. Headline deltas: the **#1030 non-directional paired-end fix** — `deduplicate_bismark_rs` + `bismark_methylation_extractor_rs` crashed on every `--non_directional` PE BAM (`read identity mismatch: expected R1 for first mate, got R2`): for CTOT/CTOB pairs Bismark deliberately swaps the SAM R1/R2 FLAG bits, and the shared `bismark_io::BismarkPair::from_mates` gate rejected the swap while Perl never inspected those bits. `from_mates` now pairs by file order + qname like Perl; **byte-identical to Perl v0.25.1** for both tools (issue reproducer + oxy 1M non-dir PE gate), and non-directional PE byte-identity cells were added to the `perl-oracle` CI job (#1031). Plus two **pluggable input front-end** backends merged since beta.12: **unaligned BAM (uBAM) input** (SE #1026 + PE #1027 + docs #1028 — transcoded to a temp FASTQ matching `samtools fastq`, byte-identical to the equivalent run) and **BINSEQ (`.vbq`) input** (#1029 — decoded in-process via the `binseq` crate, identical to `bqtools decode`; feature-gated `binseq-input`, default-OFF / release-ON). Multi-arch GHCR image `ghcr.io/felixkrueger/bismark:beta` / `:2.0.0-beta.13` + 3 platform tarballs (all 12 tools). All 12 suite tools remain byte-identical to Perl v0.25.1.
- **2026-06-25** — **`bismark-rust-v2.0.0-beta.12` released** — Rust suite beta carrying everything merged since `…beta.11`. Headline deltas: **single-cell `--add_barcode` / `--add_umi` SAM tags** (`CB:Z:` / `UR:Z:` parsed from the QNAME, #1021 — so SeekGene's `SeekSoulMethyl` can pin a tagged release and drop its v0.25.2 Perl fork; default-off + byte-identical when off), the **BGZF-compressed `--combined_index_sequential` pass-1 spill** (~7× less scratch — ~0.09 KB/pair, ~95 GB worst case for ~1 B pairs — wall-neutral + output byte-identical, #1019/#1022), and the **mimalloc global allocator** fixing `--multicore` allocator contention (macOS ~3.1× on the contended path; **performance-neutral on Linux x86_64**, #1013), plus the refreshed Rust **benchmarks** + new **"Choosing an alignment mode"** docs pages. The intervening `…beta.4`–`…beta.11` tags were incremental version bumps plus the HISAT2 `--local` / `--multicore`, rammap in-process, and combined-index v2.x work already logged below. Multi-arch GHCR image `ghcr.io/felixkrueger/bismark:beta` / `:2.0.0-beta.12` + 3 platform tarballs (all 12 tools). All 12 suite tools remain byte-identical to Perl v0.25.1.
- **2026-06-24** — `bismark` aligner **opt-in `--add_barcode` / `--add_umi` single-cell SAM tags** (`plans/06242026_umi-barcode-tags/`) — copy a cell barcode and UMI out of the read QNAME (SeekSoul `<barcode>_<umi>[_<alt>]_<name>`, split max-3 → barcode = field 0 / umi = field 1) into 10x-convention tags on `UniqueBest` records: `--add_barcode` → `CB:Z:`, `--add_umi` → `UR:Z:` (consumed by per-cell BAM splitting + `umi_tools dedup --umi-tag UR --paired`). **Both PE mates** tagged; one never-silent STDERR notice per run when a flag is set but its field is empty. **Default (no-flag) output is byte-identical** (early-return before any parse), so the existing Perl-oracle / worker-invariance gates are untouched. **Downstream-correct tags — NOT byte-identity vs the SeekGene fork**; contract verified against the live `altos-labs/SeekSoulMethyl` source. oxy real-data smoke PASS (real GRCh38: directional single-core == `--parallel 2` with both mates tagged; pbat OK; `--ambig_bam` carries 0 tags). Aligner-crate-only; dual code-review APPROVE + plan-manager COMPLETE.
- **2026-06-19** — `bismark` aligner **v2: in-process rammap `--rammap_inprocess` EXPOSED in release binaries** (`plans/06192026_rammap-release-exposure/`) — the release-exposure follow-up to the parallelization below, unblocked by oxy V6. `release.yml` (the prebuilt-binary build) + the root `Dockerfile` now compile with `--features bismark-aligner/rammap-inprocess`, so the shipped `bismark_rs` exposes a **functional** `--rammap_inprocess`. Previously the flag was **present-but-inert** on the feature-OFF release binaries (accepted, but silently ran the subprocess — the in-process path wasn't compiled in). **`--rammap` alone still defaults to the subprocess; `--rammap_inprocess` is the explicit opt-in**, and the default / local / CI builds stay feature-OFF. The feature lives only on `bismark-aligner`, so the build still produces all 14 workspace members — only `bismark_rs` is recompiled with it, leaving the **other 11 shipped binaries byte-unaffected**; the in-process path (`inprocess.rs` / merge / XM / output) is reached only via the opt-in, leaving the Bowtie 2 / HISAT2 / minimap2 / subprocess-rammap paths byte-frozen. Behaviourally `--rammap_inprocess` is **concordant — NOT byte-identical — to the subprocess** (≤0.022 %/cell on borderline long reads), **leaner + faster** (oxy V6: 1.8× wall, −54 % RAM), and **SE + FastQ only** (a FastA `--rammap_inprocess` run falls back to the subprocess with a never-silent notice). Gated by the release dry-run (feature-ON build on all 3 binary targets + both Docker arches) + an oxy functional check (the `in-process rammap-core backend` run notice). Plan: `plans/06192026_rammap-release-exposure/PLAN.md`.
- **2026-06-19** — `bismark` aligner **v2: in-process rammap `--rammap_inprocess` PARALLELIZED** (#995, `plans/06192026_rammap-inprocess-parallel/`) — the speed half the trade-off below was waiting on. `InProcessAlignerStream` now maps a 2048-read chunk per refill in PARALLEL on a SHARED `Arc<rayon::ThreadPool>` (sized by `--multicore`), buffering in input order + serving through the unchanged `SamStream` `current()`/`advance()` — so the lockstep merge / XM / output stay byte-frozen. `--rammap_inprocess` short-circuits `pipeline()` to the single-process `run_se` (NOT the fork model — that would reload the index per worker); the selection predicate dropped its `multicore` gate (fork-worker safety via the short-circuit + the `aligner == Rammap` conjunct). rammap-core exposes no batch map API (its `parallel` feature is internal), so the parallelism is Bismark-side over the independent + deterministic per-read `map_seq` (spike-confirmed thread-safe). **oxy V6: worker-invariant (`--multicore 1` == `16` byte-identical), RSS flat ~31 GB, 11.4× over serial (1M non-dir 1382 s vs 15702 s), and now BEATS the subprocess on BOTH wall (1382 < 2451 s, 1.8×) and RAM (32.3 < 70.9 GB, −54 %)** — the "threads sharing one in-memory index" payoff, realized. Thread-invariance also proven hermetically via `Aligner::from_seqs` (no oxy). **Release stays feature-OFF** — flipping `release.yml`/Dockerfile to ship `--rammap_inprocess` is the unblocked follow-up. Gate: `plans/06192026_rammap-inprocess-parallel/GATE_V6.md`.
- **2026-06-17** — `bismark` aligner **v2 experimental: in-process rammap backend (`--rammap_inprocess`)** — the rammap in-process library-integration epic (`plans/06152026_rammap-library-integration/`; #991 stream + #992 driver-wiring + this finalize). Loads each converted `.mmi` ONCE into an `Arc<rammap::Aligner>` shared across the 2/4 strand instances (vs the subprocess fork model's 4× index loads), behind the default-OFF `rammap-inprocess` Cargo feature (optional `rammap-core` git-dep, pinned v1.1.1). **`--rammap` DEFAULTS to the proven subprocess path; `--rammap_inprocess` is the explicit opt-in** (feature-ON builds only; inert on the default feature-OFF binary). **Concordance-gated — NOT byte-identical to the subprocess `--rammap`** (the same engine via its *library* API vs its *CLI*): rammap's `map_seq` lib API diverges from the CLI on ~1–2 borderline long-ONT reads per 10k (thread count / `-K` batching / wiring all ruled out). The oxy gate (4 cells × 1M: EM-ONT non-dir/dir/pbat + short-read WGBS) found **≤0.022 %/cell divergent, one-directional + conservative** (0 reads gained — never invents an alignment; residual=0, 100 %-attributed; determinism confirmed both arms; **WGBS short reads = 0 divergence** → the divergence is long-read-chaining-intrinsic). **⚖️ RAM-vs-speed trade-off:** the in-process path is **−56 % RSS** (31 vs 71 GB co-resident at 1M) but **~6.4× SLOWER** (the lockstep merge calls `map_seq` serially, single-threaded; the subprocess runs 4 instances × `-t 2` = 8-way parallel) — the "threads sharing one index" *speed* win is NOT yet realized. So it ships as a **lean-RAM / slower** compile-time opt-in; **release binaries stay feature-OFF** (`--rammap` = subprocess there) until the follow-up parallelizes the in-process `map_seq` (rammap-core has a `parallel` feature). The Bowtie 2 / HISAT2 / minimap2 / subprocess-rammap paths + the merge/XM/output are byte-frozen (every change is a feature-gated arm, a behaviour-neutral generic bound, the default-flip, or a notice). Gate: `plans/06152026_rammap-library-integration/phase3-byte-identity-gate-rss/GATE_RESULT.md`.
- **2026-06-15** — `bismark` aligner **v2 experimental: `--rammap` backend** — adds `Aligner::Rammap`, a 4th alignment backend that is a subprocess clone of the existing minimap2 seam: rammap (`jwanglab/rammap`) is the **pure-Rust minimap2 reimplementation**, spawned as an external binary via `--path_to_rammap` (no new Cargo dependency, no workspace build change). **Opt-in `--rammap`, never-silent, concordance-gated — NOT byte-identical to minimap2** (a separate v2 model, not the faithful byte-identity gate). "Minimap-like" at every site: identical SAM tag set (incl. the ignored lowercase `s2:i:` → `second_best=None`, no parser change), single `.mmi` index, clean-slate `-a --MD --secondary=no -t 2 -x map-ont -K 250K` options, SE-only, and the same `--local` / paired-end / `--combined_index` rejects (each reject names `--rammap` via `aligner.name()`). Reuses the minimap-family `--mm2_*` knobs + read-length cutoff for apples-to-apples comparison vs `--minimap2`. `--version` parses `rammap 1.1.1` (last whitespace token; `PINNED_RAMMAP_VERSION = "1.1.1"`, warn-only). `--multicore N` is accepted (the fork+chunk path is aligner-agnostic) and worker-invariant (rammap is per-read-independent + deterministic). The never-silent notice fires in `lib.rs::run()`. The Bowtie 2 / HISAT2 / minimap2 paths are byte-frozen (every change is a new `Aligner::Rammap` match arm or a `matches!(Minimap2 | Rammap)` generalization of a minimap2-only guard). Concordance vs minimap2 is the Phase-4 gate. Plan: `plans/06142026_rammap-em-ont-benchmark/phase3-rammap-backend/`.
- **2026-06-14** — `bismark` aligner **v1.x: HISAT2 `--local` mode** — extends the Bowtie 2 `--local` epic (#981) to the **HISAT2** backend, byte-identical to Perl v0.25.1 `--hisat2 --local`. HISAT2-local is **not** a `--local` passthrough (Perl 7904: "does not work with HISAT2") — it is defined by *subtraction*: drop `--no-softclip` (soft-clipping allowed) + the **same** L-form `--score-min L,0,-0.2` as end-to-end + the local `ln()` MAPQ with the `(0,−0.2)` L-form intercept/slope, and does **not** push `--local`. The load-bearing edit is `score_min_params(cli, aligner)` (G-form `(20,8)` only for `--local`+Bowtie 2, else L-form `(0,−0.2)`); `calc_mapq` is byte-frozen (sign-agnostic). Reuses #981's local MAPQ ladder + the proven `ln()` Perl≡Rust parity (no new spike). **minimap2-`--local` stays rejected — minimap2 is local (soft-clipping) by design** (no end-to-end vs local distinction). **oxy gate PASS 7/7** byte-identical to Perl `--hisat2 --local` (SE dir/non-dir/pbat + `--local --multicore` + PE, 1M GRCh38, HISAT2 2.2.2); the `--no-softclip`-drop is exercised live — end-to-end produces **0** soft-clips vs local's **28,222**. Bowtie 2-local + HISAT2 end-to-end byte-frozen. Plan + spike-reuse + gate: `plans/06142026_aligner-hisat2-local/`.
- **2026-06-14** — `bismark` aligner **v1.x: HISAT2 `--multicore N`** — un-rejects `--hisat2` with `--multicore N > 1` (the conformance suite's GAP-2). The fork+chunk `--multicore` model is **not faithful for HISAT2** (splice-site discovery is not chunk-invariant — Perl is not worker-invariant either: single-core 1310 spliced vs `--multicore 8` 1219 on a 1M oxy subset), so `--hisat2 --multicore N` is instead routed to a **single HISAT2 instance with `-p N --reorder`** (Approach B-faithful), reusing the existing `-p`/`--reorder` plumbing: **byte-identical to Perl `--hisat2 -p N`** per N (a Phase-0 oxy spike confirmed `-p N` is deterministic run-to-run but ≠ single-core — records 844,267→844,316 / spliced 1310→1298 as N grows; HISAT2 builds its splice DB in thread order). Deterministic per N but **N-/node-dependent** (HISAT2's nature) — a **deliberate, documented `--multicore`→`-p` remap for HISAT2 only**, announced by a never-silent run notice; methylseq's auto-derived `--multicore = cpus/3` now works directly (no cpus-cap workaround). An explicit `-p` alongside `--multicore` fails loud (ambiguous). Bowtie 2 `--multicore` (worker-invariant fork, Phase 9b) and single-core HISAT2 are byte-frozen. **oxy gate PASS 7/7** byte-identical to Perl `--hisat2 -p N` (SE dir N=2/4/8 + non-dir + pbat + `--ambig_bam` main&ambig + PE, 1M GRCh38, HISAT2 2.2.2). Closes conformance GAP-2 (the `KnownUnsupported` row flips to accept). Plan + spike + gate: `plans/06132026_aligner-hisat2-multicore/`.
- **2026-06-14** — `bismark_methylation_extractor` + `coverage2cytosine` **graceful no-alignment sample** — the follow-on to the dedup fix below. A header-only/no-alignment sample (zero methylation calls) flowed past the fixed dedup but then crashed nf-core/methylseq at `BISMARK_METHYLATIONEXTRACTOR` (`Missing output file(s) *.bedGraph.gz`) and would next die at `BISMARK_COVERAGE2CYTOSINE`. On **zero total calls** the extractor now emits an empty `.bedGraph.gz` + `.cov.gz` and force-creates the empty per-context `.txt.gz` (instead of skipping/deleting), satisfying methylseq's required-output globs across SE/PE and `--multicore`; coverage2cytosine now produces a genome-wide **all-zero** report on a validly-read empty `.cov` (instead of erroring), scoped to the standard path (`--nome-seq`/`--gc`/`threshold>0` still guard). Both are **deliberate, gated divergences from Perl v0.25.1** (verified: Perl extractor skips/deletes, Perl c2c dies on empty `.cov`) — non-empty runs stay byte-identical; a has-calls-but-no-CpG run still legitimately skips. Full local cascade (dedup→extractor→c2c) exits 0 with every required output. Plan + reviews: `plans/06142026_empty-sample-extractor-c2c/`.
- **2026-06-13** — `deduplicate_bismark` **graceful zero-alignment input** — fixes an nf-core/methylseq drop-in crash. A header-only BAM (e.g. a sample where nothing aligned) made the Rust dedup exit non-zero (`input file is empty`), aborting the pipeline at `BISMARK_DEDUPLICATE`. It now emits a valid header-only deduplicated BAM + a zero-count `deduplication_report.txt` (rendering `0 (0.00%)`, not `N/A%`) and exits 0, uniformly across all 8 entry points (SE/PE × single/`--multiple` × `--parallel` × UMI). A **deliberate, documented divergence from Perl v0.25.1**, which itself dies on empty input (`bam_isEmpty`) — acceptable standalone, but it must not crash methylseq. The downstream Rust extractor already handles a header-only BAM gracefully, so the chain survives. Plan + reviews: `plans/06132026_dedup-empty-input/`.
- **2026-06-13** — `bismark` aligner **v1.x: Bowtie 2 `--local` mode** — un-rejects `--local`
  (the documented `params.local_alignment`), byte-identical to Perl Bismark v0.25.1 `--local`. A
  faithful port of a different Perl mode (Bowtie 2 does the local alignment; the Rust work is the
  flags + MAPQ): emit `--local --score-min G,<i>,<s>` (default `G,20,8`); `calc_mapq` gains a
  `local` branch — `scMin = i + s·ln(readLen)` + the separate local MAPQ ladder (Perl 4082-4178).
  A Phase-0 spike proved Perl `log` ≡ Rust `f64::ln()` **bit-identical** on the gate arch (0 ULP /
  152,709 cases), so the exact-`f64` MAPQ comparisons are safe. Soft-clip `S` handling was already
  in place (`methylation.rs`). Scope: Bowtie 2 + SE/PE × dir/non-dir/pbat; HISAT2/minimap2-local +
  `--local`+combined-index rejected fail-loud. Closes the conformance suite's GAP-1 (its
  `KnownUnsupported` `--local` row flipped to accept). Plan + spike: `plans/06132026_aligner-local-mode/`.
- **2026-06-13** — **methylseq CLI-surface conformance suite** — a per-crate `tests/methylseq_conformance.rs` in all 7 Bismark-module crates (align, deduplicate, methylationextractor, coverage2cytosine, report, summary, genomepreparation) asserts each Rust CLI **accepts every command shape nf-core/methylseq 4.2.0 emits**, so a drop-in gap is caught by CI instead of a production run (the recurring class: aligner `--bam`, c2c `--genome`, extractor `--CX` were each found one-at-a-time in production). Tiered by what each crate exposes: clap-parse for all 7; `validate()` for the 4 with it; the aligner's two deliberate v1 rejects (`--local`, `--hisat2`+multicore) asserted via the pub `build_aligner_options`/`config::resolve` as **message-specific, flip-detecting** KnownUnsupported rows. Test-only (no production change). The two known limitations (`--local` unsupported; `bismark_hisat` needs `--multicore 1`) are documented under Installing → Container; their real fixes are deferred follow-ups (a `--local` v1.x epic + a HISAT2-multicore aligner task).
- **2026-06-12** — `bismark_methylation_extractor` **v1.x: accept `--CX` with `--bedGraph` (no `--cytosine_report`)** — fixes an nf-core/methylseq drop-in gap. methylseq emits `bismark_methylation_extractor --bedGraph --counts --gzip --report -s --CX …` (no `--cytosine_report`) and then runs `coverage2cytosine --CX` as a separate step on the all-context `.cov.gz`; the Rust CLI was stricter than Perl and rejected this. The guard now mirrors Perl `bismark_methylation_extractor:1259` (`die … unless ($cytosine_report or $bedGraph)`) and the `--zero_based` sibling: `--CX` makes the coverage/bedGraph cover **all** C-contexts (CpG + CHG + CHH); `--CX` with neither `--bedGraph` nor `--cytosine_report` is still rejected. Validation relaxation only — the all-context bedGraph aggregation (the R4 tee gate) was already implemented + Perl-byte-identical (the `--cytosine_report --CX` path shipped 2026-06-03). New e2e test diffs `--CX --bedGraph` against the standalone `bismark_bedgraph --CX` oracle (byte-identical) + asserts non-CpG content. Error variant `CxRequiresCytosineReport` → `CxRequiresBedgraphOrCytosineReport`. (3rd CLI-surface drop-in gap of this shape after the aligner `--bam` + c2c `--genome`, beta.3 — none catchable by byte-identity tests.)
- **2026-06-12** — `bismark` aligner **v2.x `--combined_index_sequential` for HISAT2 (SE + PE)** (combined-index v2.x Phase 7) — extends the **faithful sequential** low-RAM execution model (shipped Bowtie 2-only, #959 SE / #974 PE) to the **HISAT2** backend, for both SE and PE non-directional combined-index. The sequential model is **faithful + aligner-agnostic** — it runs model (a)'s two both-strands passes ONE AT A TIME (pass 1 spills to disk + its aligner exits, freeing the combined index, before pass 2 spawns), replaying pass 1 from disk → **BYTE-IDENTICAL to the parallel model (a)** at ~half the peak RSS. The whole change is small because the sequential machinery already threads `config.aligner` into the spawn: **lift one config-gate conjunct** (`aligner != Bowtie2` → `!matches!(Bowtie2 | Hisat2)`) + parametrize **two latent "Bowtie 2" literals** the gate relax newly exposes — the SE sequential banner AND **`AlignerStream::finish()`** (the SE stream lacked the `aligner` field its PE sibling `PairedAlignerStream` got in Phase 5 — the asymmetric-sibling back-port: a HISAT2 SE failure would have misreported "Bowtie 2"). `--combined_index_single_pass` (model b) **stays Bowtie 2-only** (its qname-tag RNG mechanism is Bowtie-2-specific). `merge`/`methylation`/`output`/`select`/`route`/`drive_merge` byte-unchanged. New tests incl. PE+SE **secondary-spill round-trip** (a HISAT2 contiguous FLAG-0x100 secondary on the C→T/pass-1/spilled stream must round-trip — the one HISAT2-specific record shape the Bowtie 2 gate never exercised; locked non-vacuously: a dropped secondary flips the MAPQ runner-up). **oxy gate PASS** (1M Sherman non-dir PE, full-GRCh38 combined `.ht2l`, HISAT2 2.2.2): HISAT2 sequential **md5-IDENTICAL to HISAT2 model (a)** (@PG-filtered) + **RSS 9.92 GB / 1 `hisat2-align` co-resident vs model (a) 19.84 GB / 2** (−50 %; HISAT2's `.ht2l` is larger than Bowtie 2's `.bt2l`, so the absolute win exceeds the BT2 gate). **→ `--combined_index` now offers the faithful sequential low-RAM mode for Bowtie 2 AND HISAT2, SE + PE.** (HISAT2 PE single-pass model (b) remains a non-goal — model (b) is Bowtie-2-specific by nature.)
- **2026-06-11** — `bismark` aligner **v2.x `--combined_index` paired-end NON-DIRECTIONAL low-RAM variants** (combined-index v2.x Phase 6) — adds the two opt-in **paired-end** non-directional execution models that halve model (a)'s two-co-resident-index peak RSS (the PE analogs of the shipped SE variants #958/#959), **Bowtie 2-only**: (1) **`--combined_index_sequential`** — the **faithful** model: run model (a)'s two both-strands PE passes ONE AT A TIME (pass 1 C→T spills to a temp file via the new `align::PairedFileSamStream` + its Bowtie 2 **exits**, freeing the index, before pass 2 G→A spawns), replaying pass 1 from disk into the **byte-frozen, already-`<C,G>`-generic** `drive_merge_combined_pe_nondir` (Phase-3 foresight) → **BYTE-IDENTICAL to parallel model (a)**, so it inherits model (a)'s validation; (2) **`--combined_index_single_pass`** (model b) — ONE both-strands PE pass over conversion-TAGGED interleaved mate files (`<base>__CT/1/1`, the tag BEFORE the mate suffix — load-bearing for `SamPair::from_lines`), split by the tag-stripped `seq_id` into the C→T (OT/OB) + G→A (CTOT/CTOB) groups → the **reused** `select_and_route_pe_nondir`; **NOT byte-identical AND NOT decision-equivalent** (the qname tag perturbs Bowtie 2's read-name RNG), opt-in, never the default, own ground-truth gate. New `convert_pe_tagged_interleaved` (two tagged mate files; PE-FastA never gzips) + `spill_pe_stream_to_file` + the tagged driver's 3-part never-silent guard (`assert_pe_tag_in_sync`: head tag-stripped `seq_id` == identifier, both halves non-empty, untagged record dies). config lifts only the two `layout.is_paired()` rejects (NonDir/Bowtie2/mutex kept — PE-HISAT2/directional/pbat + flag still reject loudly); `merge`/`methylation`/`output`/`route_pe_decision`/`select_core_pe` byte-unchanged. **oxy gate PASS** (1M Sherman non-dir PE, full-GRCh38 combined `.bt2l`): **sequential = md5-IDENTICAL to model (a)** (@PG-filtered) + **RSS 7.83 GB / 1 `bowtie2-align` co-resident** (vs model (a) 15.74 GB / 2); **single-pass (b) = 100.0000 % decision-agree / 0 churn, FLAG/XM 0-mismatch, truth-accuracy identical** (99.77 %/99.47 % MAPQ≥23) + **RSS 7.83 GB / 1** — well inside the pre-registered ≥99.99 %-agree / ≤0.05 pp bar (0-churn is stronger than the SE spike's ~98/1M; PE pair-sum scoring resolves the co-optimal ties the RNG tag would otherwise perturb). **→ `--combined_index` PE now offers the same −50 %-RAM sequential + single-pass modes as SE; the combined-index v2.x epic is fully complete (SE + PE × Bowtie 2 + HISAT2 × dir/non-dir/pbat × parallel + low-RAM).**
- **2026-06-11** — `bismark_methylation_extractor` **v1.x: multiple input files + single-end coordinate-sorted input** — `bismark_methylation_extractor_rs --bedGraph *bam` now works on coordinate-sorted single-cell SE BAMs. (1) **Multiple input files** loop per-file with full per-file state (split files / M-bias / splitting report / bedGraph / coverage / cytosine report all named from each file's basename) — no cross-file pooling, faithful to Perl's `foreach my $filename`; fail-fast on first error. (2) **Single-end coordinate-sorted input accepted** — the SE/PE distinction *is* the sort-check policy (SE uses `bismark_io::open_reader_without_sort_check` / `*_from_path_without_sort_check`; PE keeps the hard `UnsortedInput` header rejection), mirroring Perl, which only sort-checks paired-end (`test_positional_sorting` gated `if ($paired)`). bismark-io gains additive `BamReader`/`SamReader::from_path_without_sort_check` + `open_reader_without_sort_check` (no semver bump). Per-context split files preserve input-record order (so only the position-aggregated bedGraph/.cov + count-based reports are order-invariant across reorderings); byte-identity oracle is Perl on the same coordinate-sorted file. Real-data gate vs Perl v0.25.1 PASS (coord-sorted 10M_SE: all split files + bedGraph + cov + M-bias byte-identical, single-file AND multi-file per-file).
- **2026-06-11** — `bismark` aligner **v2.x `--combined_index` paired-end HISAT2 — combined-index v2.x epic COMPLETE** (Phase 5, #973) — extends PE `--combined_index` to **HISAT2**, all 3 library types (the LAST phase). The functional unblock is **one config-gate conjunct** (`aligner == Bowtie2` → `matches!(Bowtie2 | Hisat2)`): the PE combined machinery is aligner-agnostic — `PairedAlignerStream::spawn` runs whichever `detected_aligner` binary resolves, and classify/select/route + `select_core_pe`'s runner-up-**pair** MAPQ never read the aligner (so the faithful PE-HISAT2 read-1-`ZS`-drop is oracle-side only — no combined reconciliation). Plus a **byte-neutral** spawn de-hardcode: `build_pe_argv` (Bowtie 2 + HISAT2 share the PE argv shape; minimap2 PE `unreachable!`) + aligner-aware `spawn`/`finish` errors (never-silent). `lib.rs` threads `config.aligner` to the 4 PE spawn callers — **+4 lines** (the reuse thesis); `merge`/`methylation`/`combined`/`output`/`mapq` byte-unchanged. HISAT2's runner-up surfaces as a contiguous secondary PAIR (FLAG 0x100); the existing gather + `select_core_pe` consume it (MD only for tied best-sum pairs — the C1 trip-wire, covered by an MD-abort e2e). Opt-in, never-silent, **concordance-gated**; minimap2 PE combined stays rejected. **oxy gate PASS** (HISAT2 2.2.2, full-GRCh38 combined `.ht2l` built this session + persisted to S3) vs the faithful HISAT2 PE oracle, all 3 lib types (1M Sherman each): **FLAG-pair 0-mismatch**; **MAPQ≥23 acc combined 99.80% ≈ oracle 100%** chr; churn ~0.19%; pbat ~42% map-eff (CTOT/CTOB exerciser). **XM-pair mm ~12/845k (0.0015%) = 100% HISAT2 splice-CIGAR co-optimal choice at the same POS** (machine-confirmed; not miscalls — the HISAT2-splicing analog of the Bowtie 2 churn, structurally absent in Bowtie 2 → why the BT2 phases hit XM-0); with the existing `--no-spliced-alignment` flag (inherited by both faithful + combined) the XM mm drops to **0** — splicing was 100% the cause. **→ combined-index v2.x epic COMPLETE: `--combined_index` now covers paired-end + single-end, Bowtie 2 + HISAT2, directional / non-directional / pbat — all concordance-gated.**
- **2026-06-11** — `bismark` aligner **v2.x `--combined_index` paired-end PBAT mode** (combined-index v2.x Phase 4, #972) — extends the PE combined-index path to **pbat** (Bowtie 2), the **last PE Bowtie 2 library type**: the G→A-pass half of non-directional, **standalone** — ONE both-strands PE pass over `BS_combined` on the G→A-converted reads (`-1 G→A_R1 -2 C→T_R2`), `-k 2`, classified to CTOT/CTOB via `classify(ReadConv::Ga,…)` and resolved by the **reused, unchanged** 4-slot `select_core_pe` + the byte-frozen `route_pe_decision`. New thin `select_pe_pbat` (the PE analog of SE `select_pbat`); a `SelectFnPe` fn-pointer parametrizes `process_pe_chunk_combined` + `drive_merge_combined_pe` so the **directional + pbat single-stream PE paths share one driver** (directional behaviour-identical). Opt-in, never-silent, **concordance-gated (NOT byte-identical)**; PE-HISAT2 pbat + the low-RAM exec models stay rejected (later phases). **No `+2`/pbat-param in PE** — `route_pe_decision` has no pbat param and `extract_corresponding_genomic_sequence_paired_end` maps index 1/2 natively, so the SE pbat double-shift gotcha has no PE analog. **oxy gate PASS** vs the faithful 2-instance PE `--pbat` oracle (3 cells): the **non-dir Sherman + `--pbat` CTOT/CTOB exerciser** — map-eff combined 48.0%/oracle 47.7%, combined maps the **same ~475k read set** as the oracle (churn 0.22%, **FLAG-pair/`XM`-pair 0-mismatch**) = genuine pbat selection; mate-swap Sherman (churn 0.20%, 0 mm/950,626) + mate-swap real WGBS (churn 0.47%, 0 mm/847,699); **MAPQ≥23 truth-accuracy ≈ oracle** (99.77%/99.46% vs 99.98%/99.97%) in the Phase-2/3 low-MAPQ-tail profile. No RAM measurement (single PE pass — one combined index resident). (Gate note: mate-swap+`--pbat` round-trips to OT/OB via the `--pbat ≡ swap` identity → a concordance test; PE strand is not single-mate-XR/XG-readable, so pbat selection is proven by map-eff + same-read-set.) The faithful default + all SE + Phase-2/3 PE combined paths untouched. **Only PE-HISAT2 (Phase 5) remains in the v2.x epic.**
- **2026-06-11** — `bismark` aligner **v2.x `--combined_index` paired-end NON-DIRECTIONAL mode** (combined-index v2.x Phase 3, #970) — extends the PE combined-index path to non-directional (parallel **model (a)**): **two** both-strands PE passes over `BS_combined` (C→T reads → OT/OB, G→A reads → CTOT/CTOB), `-k 2` each, **unioned per read pair** via the **reused** 4-slot `select_core_pe` tie machine (`[0,3,1,2]` scan order) + the byte-frozen `route_pe_decision` (both unchanged from Phase 2 — the directional phase front-loaded the 4-slot correctness, locked by `select_core_pe_uses_literal_scan_order_not_ascending`). New `select_pe_nondir` + two-stream driver (`drive_merge_combined_pe_nondir`, desync guard keyed on `SamPair::seq_id`) mirror the SE non-dir path doubled. Opt-in, never-silent, **concordance-gated (NOT byte-identical)**; PE pbat/HISAT2 + the single-pass/sequential low-RAM exec models stay rejected (later phases). **oxy gate PASS** vs the faithful 4-instance non-dir PE oracle (1M Sherman non-dir PE): **FLAG-pair/`XM`-pair 0-mismatch** on 950,673 POS-concordant pairs (incl. the new CTOT/CTOB); **MAPQ≥23 truth-accuracy ≈ oracle** (combined 99.78%/99.47% vs 99.98%/99.97% chr/within-fragment); churn 0.206% in the self-flagging MAPQ<23 multimapper tail. **Peak RSS 15.71 GB / 2 `bowtie2-align` co-resident (model (a) two-index signature) — the Phase-6 data-gate input** (a PE sequential variant would cut ~−50%, as the SE one did). The faithful default + all SE + Phase-2-directional combined paths untouched.
- **2026-06-11** — `bismark` aligner **v2.x `--combined_index` paired-end directional mode** (combined-index v2.x Phase 2, #969) — extends the combined-index path to **paired-end** (directional, Bowtie 2): ONE both-strands `-1/-2` pass over `BS_combined` (`-k 2`, `Orientation::Both`) instead of two strand-restricted PE instances, classified **per pair on read-1's orientation × sub-genome** into the byte-frozen PE output arm via a new PE slot map (`to_index_pe`: OT=0/CTOB=1/CTOT=2/**OB=3** — the OB↔CTOB 1↔3 swap vs the SE numbering), best-by-**summed-AS** with the shared `select_core_pe` tie machine (literal `[0,3,1,2]` scan order). `route_pe_decision` extracted from the faithful `drive_merge_pe` (pure relocation, reused by both drivers). Opt-in, never-silent, **concordance-gated (NOT byte-identical)**; non-dir/pbat/HISAT2/minimap2/multicore PE combined stay hard-rejected. **oxy gate PASS** vs the faithful 2-instance directional PE oracle (1M real WGBS PE + 1M clean Sherman PE): **FLAG-pair/`XM`-pair 0-mismatch** on ~850k/~950k POS-concordant pairs; **MAPQ≥23 truth-accuracy ≈ oracle** (combined 99.78%/99.46% vs 99.98%/99.97% chr/within-fragment); churn 0.21%(Sherman)/0.46%(real) is inherent to PE + concentrated in the self-flagging MAPQ<23 multimapper tail. The faithful default + all SE combined paths untouched. Phases 3 (PE non-dir) / 4 (PE pbat) / 5 (PE-HISAT2) next.
- **2026-06-09** — **`bismark-rust-v2.0.0-beta.3` released — nf-core/methylseq drop-in compatibility** — closes the 3 gaps an end-to-end **nf-core/methylseq 4.2.0** proof run exposed (output-byte-identity tests couldn't find these — only running the real pipeline did): the container now ships **`procps`** (Nextflow's task wrapper shells out to `ps` for every task's metrics), the **aligner accepts `--bam`** (the Perl flag methylseq passes; the modernized CLI had dropped it for BAM-default — re-added as an accepted no-op), and **`coverage2cytosine` accepts `--genome`** (alias of `--genome_folder`, which Perl took via Getopt prefix-match). With these, the beta is a **proven byte-identical drop-in for methylseq**: a `withName:'.*BISMARK_.*' { container = … }` swap yields **20/20 identical** methylation outputs (dedup BAM records / `.cov` / `.bedGraph` / CpG-report / M-bias) + identical genome-prep + splitting-report data vs the stock Perl `bismark:0.25.1` container, across 4 samples. No methylation/alignment logic changed (CLI + container only).
- **2026-06-09** — **`bismark-rust-v2.0.0-beta.2` released** — second public beta of the Rust suite (multi-arch GHCR image `ghcr.io/felixkrueger/bismark:beta`/`:2.0.0-beta.2` + 3 platform tarballs, all 12 tools). Delta since beta.1 (2026-06-06): the **v2 `--combined_index` epic** (directional / non-dir model (a) / pbat / single-pass model (b) / **sequential**, #955–959) + the **canonical-name container** (#960) — a zero-edit nf-core/methylseq drop-in (tools exposed under canonical names; `bismark -v` byte-identical to the Perl v0.25.1 version oracle). All 12 suite tools remain byte-identical to Perl v0.25.1.
- **2026-06-08** — `bismark` aligner **v2 `--combined_index_sequential` — faithful sequential non-directional memory mode** (#959) — the **faithful** counterpart to model (b) (#958): runs model (a)'s two both-strands non-dir passes **SEQUENTIALLY** — pass 1 (C→T) spills its records to a temp file and its Bowtie 2 **exits** (freeing the combined index) before pass 2 (G→A) spawns — replaying pass 1 from disk via a new file-backed `SamStream` (`FileSamStream`) and unioning per read via the EXISTING `combined::select_nondir`. **One combined index resident at a time → −50% peak RSS**, the same memory win as model (b) but **BYTE-IDENTICAL** to the default parallel model (a): Bowtie 2's output is independent of *when* each pass runs (exec-model spike control C2) and both feed the same UNTAGGED converted files → it **inherits model (a)'s validation** (no Sherman accuracy gate needed). `drive_merge_combined_nondir` reused with a body-unchanged, **signature-only** generic widening (`<S>`→`<C, G>`); `merge.rs`/`methylation.rs`/`output.rs`/`combined.rs` + the model (a)/(b)/directional/pbat paths byte-unchanged. Opt-in, never-silent, **mutually exclusive with `--combined_index_single_pass`**. **oxy gate PASS** (1M Sherman, both modes from one binary): BAM records byte-identical to model (a) (**929,141 recs, same md5**; unmapped + ambiguous identical) + **RSS 7.82 GB / 1 `bowtie2-align-l` vs model (a) 15.70 GB / 2** (ratio 0.498); wall 1.97× (passes serial — the trade). Sequential's 7.82 GB == model (b)'s, but faithful. **Completes the combined-index v2 epic** (all 3 SE library types + both memory modes — non-faithful model (b) + faithful sequential). **NON-DIR ONLY**. rammap / avenue-B remain deferred.
- **2026-06-08** — `bismark` aligner **v2 `--combined_index_single_pass` — non-directional model (b) single-pass memory mode** (#958) — an opt-in single-pass execution model for `--combined_index --non_directional`: ONE Bowtie 2 pass over conversion-TAGGED interleaved reads (`<id>__CT` C→T + `<id>__GA` G→A) over `BS_combined`, split by the qname tag back into the C→T (OT/OB) + G→A (CTOT/CTOB) groups and fed to the EXISTING `combined::select_nondir` union — **one combined index load instead of model (a)'s two (−50% peak RSS)**. **NOT byte-identical AND NOT decision-equivalent** to model (a): the qname tag perturbs Bowtie 2's read-name-seeded RNG (~98/1M co-optimal reads pick differently — benign + symmetric) — so it is explicitly opt-in, never the default, never silently substituted, and ground-truth-validated on its own (the model-(b) accuracy spike). NEW `convert_se_tagged_interleaved` core (the frozen single-kind converters untouched) + a one-stream/split-by-tag driver; the shared per-read tail extracted to `select_and_route_se_nondir` (both model (a) + (b) drivers). **oxy gate PASS** (1M Sherman balanced reads): **RSS 7.82 GB / 1 `bowtie2-align-l` vs model (a) 15.70 GB / 2** (ratio 0.498 — machine-asserted co-residency, the silent-no-op guard); Sherman accuracy 99.9631% ≈ oracle 99.9663% (|Δ| 0.0032 pp); decision agree-rate vs model (a) 99.9958%; §4b same-pos-strand 0. `merge.rs`/`methylation.rs`/`output.rs`/`combined.rs` byte-unchanged; the faithful default + model (a) + directional/pbat paths untouched. **NON-DIR ONLY** (the sole 2-index-load mode). rammap / avenue-B remain deferred.
- **2026-06-08** — `bismark` aligner **v2 `--combined_index` PBAT mode shipped — avenue-A COMPLETE for all 3 SE library types** (#957) — extends the combined-index path to the **third and final SE library type**, completing avenue-A (directional #955 / non-directional #956 / pbat). PBAT-combined is the **G→A-pass half of non-directional, standalone**: ONE both-strands Bowtie 2 pass over `BS_combined` fed the G→A-converted reads, `-k 2`, classified to **CTOT (2) / CTOB (3)** via `classify(ReadConv::Ga,…)` and resolved by the shared `combined::select_core` tie machine (CTOB wins a same-locus CTOT×CTOB tie, later slot). Routes `route_se_decision(pbat=FALSE)` — `classify` emits index 2/3 directly, so the faithful `+2` modifier (`pbat=true`) would double-shift to eff 4/5 (fail-loud, never a silent miscall). The directional `drive_merge_combined`/`process_se_chunk_combined` were parametrized with a `SelectFn` fn-pointer (one shared gather loop; behaviour-identical for directional) rather than triplicated. **oxy `--pbat` gate PASS** vs the faithful 2-instance oracle (same binary, flag off) on 1M Sherman balanced reads, `-k 2`: oracle-unique-stays-unique 99.9557% (churn 0.0443%); **CTOT POS/`XM`/FLAG/`XR`/`XG` = 100%**, CTOB ≥99.99828% + =100% among POS+strand-concordant; **OT/OB empty (n=0)** — the PBAT signature; §4b same-pos-strand divergence 0; Sherman position accuracy combined 99.9546% ≈ oracle 99.9561% (|Δ| 0.0015 pp). `merge.rs`/`methylation.rs`/`output.rs` byte-unchanged; the faithful default + directional/non-dir combined paths untouched. Model (b) (single tagged invocation, the −50% RSS memory mode) shipped next (#958); rammap/avenue-B remain deferred.
- **2026-06-08** — `bismark` aligner **v2 `--combined_index` NON-DIRECTIONAL mode shipped** (#956) — extends the combined-index path (#955) to non-directional: **two** both-strands Bowtie 2 passes over `BS_combined` (C→T reads → OT/OB, G→A reads → CTOT/CTOB), `-k 2` each, **unioned per read**; read-conversion-aware classification + the shared `chr:pos`+`>=`+Sylvain-Foret tie machine (`combined::select_core`) across all four synthetic indices (OT=0/OB=1/CTOT=2/CTOB=3), **CTOB winning the §4b telomeric OT×CTOB same-position collision** (later slot). Model **(a)** (two parallel passes + per-read union); the single conversion-tagged-invocation model (b) was spike-rejected (the qname tag perturbs Bowtie 2's read-name-seeded RNG → not decision-equivalent) and deferred to v2.x. **oxy non-dir gate PASS** vs the faithful 4-instance oracle (same binary, flag off) on 1M Sherman balanced non-dir reads, `-k 2`: oracle-unique-stays-unique 99.978% (churn 0.022%); 4 strands balanced; per-strand exact-POS/`XM` ≥99.9987% (incl. the new CTOT/CTOB); `XR`/`XG`/FLAG ≥99.99% + =100% among POS-concordant; §4b same-pos-strand divergence 0; Sherman position accuracy combined 99.9639% ≈ oracle 99.9663% (|Δ| 0.0024 pp). **PBAT combined shipped next** (#957). The faithful default + the directional combined path are untouched.
- **2026-06-08** — `bismark` aligner **v2 `--combined_index` directional mode shipped** (#955) — opt-in, never-silent, **concordance-gated (NOT byte-identical)**: one both-strands Bowtie 2 pass over the combined CT+GA index (`Bisulfite_Genome/Combined/BS_combined`) instead of separate per-strand instances, recovering strand from RNAME-suffix×FLAG (OT→synthetic index 0, OB→1) into the byte-frozen output arm; faithful `chr:pos`+`>=`+Sylvain-Foret same-position tie resolution. **Single-end directional only** (non-dir/pbat/PE/HISAT2/minimap2/multicore hard-rejected); the faithful default path is untouched. **oxy directional gate PASS** vs the faithful 2-instance oracle (same binary, flag off) on real GRCh38 WGBS-SE at full 84M: oracle-unique-stays-unique 99.9008%; per-strand exact-POS OT 99.99491% / OB 99.99501%; `XM` OT 99.99583% / OB 99.99580%; `XR` 100% both strands; `XM`-among-POS-concordant 100% (no methylation divergence beyond placement). Non-directional = future phases.
- **2026-06-06** — `bismark` aligner **v1.x minimap2 SE backend + combined 10M gate — v1.x epic COMPLETE** (#950). minimap2 single-end byte-identical to Perl v0.25.1 + minimap2 2.31-r1302 (a pure wrapper: clean-slate `-a --MD --secondary=no -t 2 -x map-ont -K 250K` + positional `.mmi`; the merge/MAPQ/XM core reused unchanged — minimap2's `s2:i:` is ignored → `second_best=None`). PE-minimap2 hard-rejected (the Perl PE path is unfinished WIP — documented known gap). **Phase-5 combined 10M single-core gate: all 13 cells byte-identical** — HISAT2 SE+PE × {dir, non-dir, pbat}, minimap2 SE × {dir, non-dir, pbat} (worker-invariant `--parallel 8`==`1` @10M), Bowtie 2 SE/PE anchors, **+ mouse GRCm39 RRBS** (HISAT2 PE 11.73M rec + Bowtie 2 PE 12.56M rec); `ht2_pe_pbat` via R1↔R2 swap = real pbat (16.25M rec). The Bismark aligner now supports **Bowtie 2 + HISAT2 (SE+PE) + minimap2 (SE)**, all byte-identical to Perl driving the same pinned aligner.
- **2026-06-04** — `bismark` aligner **Phase 10 (full-scale real-data gate) PASSED — faithful aligner epic COMPLETE.** On oxy vs Perl v0.25.1 + Bowtie 2 2.5.5: content byte-identical at full real-data scale across WGBS SE (84.0M reads, 71.3M recs, 173 contigs), PE (84.0M pairs, 143.4M recs, 181 contigs), **mouse RRBS GRCm39** (46.7M pairs, 55.4M recs, 52 contigs), and **pbat** (R1↔R2 swap, 143.4M recs). Gate A (10M) = strict byte-identity vs Perl single-core + worker-invariance + *measured* multicore-multiset-invariance; Gate B (full) = content-multiset + report + count-reconciliation + RNAME-set + aux + perf; **V13** = the pre-existing Perl `--parallel 4` BAMs carry the same content md5 (four layouts converge). Dual code-review + plan-manager COMPLETE. The aligner is byte-faithful end-to-end: SE+PE, FastQ+FastA, all 3 library types, worker-invariant, full-scale-validated.
- **2026-06-03** — `bismark_methylation_extractor` **inline bedGraph/coverage2cytosine streaming** merged — `--bedGraph`/`--cytosine_report` now drive `bismark2bedGraph` + `coverage2cytosine` **in-process** (an in-memory tee feeds the bedGraph aggregator; c2c runs from the `.cov.gz`), replacing the Perl-subprocess scaffold. Byte-identical to Perl v0.25.1 across WGBS SE+PE × {bg, cr, cr_cx, cr_split, cutoff2, zero, ucsc} + RRBS `bg` at full scale (Phase H sub-gate 2, 16/16 oxy cells). `--CX` peak RSS comparable to Perl (~26–34 GB; inline is genome-bounded, not a regression at scale).
- **2026-06-03** — `bismark` aligner **Phase 9a (FastA, #942) + Phase 9b (order-preserving `--multicore`/`--parallel`)** — FastA input across all library types, plus worker-count-invariant file-level threading: `--parallel N` output byte-identical to `--parallel 1` and Perl single-core on oxy (6 cells SE/PE × {directional, non-directional, pbat}, 10k + 1M, incl. a non-divisible count). Full-scale real-data gate (Ph 10) remains.
- **2026-06-02** — `bismark` aligner **Phases 1–8** merged (#930) — SE + PE FastQ, **all 3 library types** (directional / non-directional / pbat); byte-identical to Perl v0.25.1 + Bowtie 2 2.5.5 on oxy (4 mode×layout cells, 10k + 1M reads/pairs). FastA + threading + full-scale gate (Phases 9–10) remain.
- **2026-06-02** — `coverage2cytosine` **v1.x niche modes** (`--gc`/`--nome-seq`/`--drach`/`--ffs`) merged (#934); 15-cell full-hg38 oxy gate byte-identical to Perl v0.25.1, tag `…beta.2`. **c2c port complete.**
- **2026-06-01** — `bismark2summary` ported (#932) — byte-identical project-level summary; the **last post-alignment module**.
- **2026-06-01** — `bismark2report` ported (#931) — byte-identical per-sample HTML report.
- **2026-06-01** — `filter_non_conversion` ported (#927) — byte-identical non-CpG / incomplete-conversion read filter.
- **2026-06-01** — `bam2nuc` ported (#922) — byte-identical mono/di-nucleotide coverage QC.
- **2026-06-01** — CI **`perl-oracle byte-identity`** gate added (#933) — runs the live Perl tools in CI and byte-compares.
- **2026-06-01** — `NOMe_filtering` ported (#928) — byte-identical standalone NOMe filter.
- **2026-05-31** — `bismark_genome_preparation` ported (#913) + `--genomic_composition` (#925) — byte-identical converted FASTA.
- **2026-05-31** — `coverage2cytosine` **v1.0** core merged (#892) — byte-identical CpG/CX report + `--merge_CpGs`; ~12× on the CpG-report path.
- **2026-05-30** — `bismark2bedGraph` ported (#893, epic #797) — byte-identical coverage/bedGraph; mimalloc perf (#915).
- **2026-05-30** — `methylation_consistency` ported (#896, epic #890) — byte-identical.
- **2026-05-26 → 05-29** — `bismark_methylation_extractor` ported (Phases A–G, #847–#883) — byte-identical at full scale, **~4.8×**; the ~16% runtime hot-spot.
- **2026-05-24 → 05-26** — `deduplicate_bismark` v1.2 UMI/RRBS modes (#819–#844) — byte-identical.
- _(earlier)_ — `bismark-io` shared library — the noodles BAM/SAM/CRAM foundation all consumers build on.
- ✅ **The faithful (Bowtie 2) `bismark` aligner port is COMPLETE** (all 10 phases, #948) — byte/content-identical to Perl v0.25.1 + Bowtie 2 2.5.5 across SE+PE, FastQ+FastA, all library types, full real-data scale (incl. mouse GRCm39 + pbat).
- ✅ **`bismark` aligner v1.x COMPLETE — HISAT2 (SE+PE) + minimap2 (SE)** (epic `plans/06052026_bismark-aligner-v1x/`, #949 + #950). Both backends byte-identical to Perl v0.25.1 driving the same pinned aligner; the Phase-5 combined 10M gate confirmed all 13 cells (3 backends × library types + mouse GRCm39). The Bismark aligner now offers all three engines.
- ✅ **combined-index v2.x — paired-end + HISAT2 + low-RAM variants COMPLETE** (epic `plans/06102026_combined-index-v2x/`, #967/#969/#970/#972/#973 + Phase 6 + Phase 7) — extended the SE + Bowtie 2 combined-index (#955–959) to **paired-end**, the **HISAT2** backend, and the **low-RAM execution models**, concordance-gated, parallel model (a) the default. **`--combined_index` now covers SE + PE × Bowtie 2 + HISAT2 × directional / non-directional / pbat × parallel + low-RAM (sequential / single-pass)** — all concordance-gated vs the faithful per-strand oracle (FLAG-pair 0-mismatch; MAPQ≥23 ≈ oracle; HISAT2 default-splice XM divergence is co-optimal splice-CIGAR, eliminable via `--no-spliced-alignment`). **Low-RAM variants DONE** — `--combined_index_sequential` (faithful, **md5-identical to model (a)**, **−50 % peak RSS / one index resident**) for **Bowtie 2 + HISAT2, SE + PE** (Phase 6 BT2-PE / Phase 7 HISAT2 SE+PE; oxy: BT2 7.83 GB vs 15.74, HISAT2 9.92 GB vs 19.84) + `--combined_index_single_pass` (model b, ground-truth-gated, **Bowtie 2-only** — the qname-tag RNG mechanism is Bowtie-2-specific by nature). **minimap2-combined deferred to the rammap / long-read track** (architecturally mismatched + no PE oracle).
