# `bismark-coverage2cytosine` — release checklist (SUPERSEDED)

> **Superseded by the single-binary consolidation (3.0.0).** coverage2cytosine is no longer
> a separately-released crate — it is the `bismark::coverage2cytosine` module of the one
> `bismark` crate, released as part of the suite via
> [`.github/workflows/release.yml`](.github/workflows/release.yml). There is no separate c2c
> tag or per-tool publish, and the `rust/iron-chancellor` integration branch this file
> referenced has been retired. The coverage2cytosine byte-identity gate now runs with the
> suite's `perl-oracle` CI. See [RELEASE_CHECKLIST.md](RELEASE_CHECKLIST.md) and the
> single-binary-suite epic (`plans/07062026_single-binary-suite/`).
