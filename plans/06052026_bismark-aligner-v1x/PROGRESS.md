# Progress: `bismark` aligner v1.x — HISAT2 + minimap2 wrappers

**Last updated:** 2026-06-05 (**Phase 2a SHIPPED — PR #949 open, all-CI-green** [SE byte-identical, multicore+hisat2 rejected]. **Phase 2b DONE + GATED** [PE read-1 ZS mask; gate also found+fixed a PE TLEN dovetail bug; V8 PE oxy gate PASSED 10k+1M incl. --ambig_bam raw-passthrough]; uncommitted on `rust/aligner-v1x`. **HISAT2 backend COMPLETE — SE+PE, all libraries, byte-identical.** Next: minimap2 spike [Phase 3].)

## Phase Status

| # | Phase | Status | Directory | Notes |
|---|-------|--------|-----------|-------|
| 1 | HISAT2 determinism spike | ✅ **Done — premise HOLDS** | `phase1-hisat2-determinism-spike/` | ✅ byte-deterministic (run1==run2, 8360 rec, oxy); options = Bowtie2 base + `--no-softclip --omit-sec-seq`; same 2-instance strand model (`--norc`/`--nofw`); ZS in raw stream only; 12 spliced N-CIGAR exercised; byte-identity gate reachable (no concordance fallback). `SPIKE_hisat2_determinism.md`. Depends on — |
| 2a | HISAT2 core (detect+options+discovery+naming+SE gate) 🎯 | ✅ **DONE + GATED** (impl + dual code-review APPROVE + plan-manager COMPLETE + V9 SE oxy gate PASSED; uncommitted on `rust/aligner-v1x`) | `phase2a-hisat2-core/` | `aligner.rs` multi-backend; options = Bowtie2 base + `--no-softclip --omit-sec-seq` **appended last**, **`--dovetail` suppressed** for HISAT2, splice-flags handled/die; `.ht2` **8-suffix-arity** discovery; token threaded lib.rs **+ parallel.rs**; `ReportHeader` aligner field. **SE single-core byte-identical** (10k+1M: dir/non-dir/pbat/FastA). **🔴 `--multicore`+`--hisat2` HARD-REJECTED** (HISAT2 splice discovery is batch-global → not byte-identical; Perl itself not worker-invariant). 271 tests; `CODE_REVIEW_{A,B}.md`+`COVERAGE.md`+`GATE_OXY.md`. Depends on #1 |
| 2b | HISAT2 PE (read-1 ZS fix) + PE/non-dir/pbat/FastA gate 🎯 | ✅ **DONE + GATED** (impl + dual code-review APPROVE + plan-manager COMPLETE + **V8 PE oxy gate PASSED** 10k+1M; uncommitted on `rust/aligner-v1x`) | `phase2b-hisat2-pe/PLAN.md` | PE read-1 `ZS` mask (`merge.rs:609`, single call site). **🔴 gate also found+fixed a PE TLEN `dovetail` bug** (`$dovetail` var is aligner-independent; Rust wrongly scanned aligner_options → flipped TLEN on same-POS reverse-read-1 pairs; fix `RunConfig.dovetail=!no_dovetail`). PE/non-dir/pbat/FastA + `--ambig_bam` (raw-passthrough) byte-identical to Perl v0.25.1+HISAT2 2.2.2 (1M: pe_dir 1,620,342 / pe_nondir 1,620,302 / ambig 173,420). 277 tests. `CODE_REVIEW_{A,B}.md`+`COVERAGE.md`+`GATE_OXY.md`. Depends on #2a |
| 3 | minimap2 determinism + selection spike | 📋 Planned | `phase3-minimap2-determinism-selection-spike/` | Highest-risk (OQ3): determinism + the both-strand selection arithmetic; decides byte-identity-vs-concordance. Depends on #2b |
| 4 | minimap2 wrapper + merge adaptation + gate 🎯 | 📋 Planned | `phase4-minimap2-wrapper/` | Positional `.mmi`, `-ax sr`, `/1 /2` retention, no-strand-restriction merge path. Depends on #3 |
| 5 | Combined full-scale gate + PR 🎯 | 📋 Planned | `phase5-fullscale-gate/` | HISAT2 + minimap2 full-scale on oxy; README/journal bump; fresh-branch PR. Depends on #2b, #4 |

## History

- 2026-06-05: **Phase 2b DONE + GATED (HISAT2 paired-end).** Full pipeline: implement (`/code-implementation`) → dual `/code-reviewer` **both APPROVE** (0 Crit/High) + `/plan-manager` **COMPLETE** → 🎯 **V8 PE oxy byte-identity gate PASSED** (10k all 6 cells + 1M pe_dir/pe_nondir/pe_ambig). The planned change was the **read-1 `ZS` mask** (`merge.rs:609`, single chokepoint; 4 mate-tag unit cases incl. the Case-C MAPQ-ladder flip) — verified correct by all three audits. **🔴 The oxy gate then found a SEPARATE byte-identity bug the plan + reviewers missed:** PE **TLEN sign** flipped on same-POS fully-overlapping reverse-read-1 (FLAG 83) pairs, because Rust derived the TLEN `dovetail` from `aligner_options.contains("--dovetail")` (false for HISAT2, flag suppressed in 2a) while Perl's `$dovetail` *variable* is `!no_dovetail` for ALL aligners (8047; the `if($bowtie2)` only gates the option push). Fix: `RunConfig.dovetail=!cli.no_dovetail`; `lib.rs` uses `config.dovetail` (Bowtie 2 no-op); `output.rs::pe_tlen_tree` index-3 same-POS regression cases. After the fix: byte-identical at 1M (pe_dir 1,620,342 / pe_nondir 1,620,302 / `--ambig_bam` raw-passthrough 173,420 + 4 aux). 277 tests; clippy -D + fmt clean. Folded review findings: M-1 (unconditional-mask §8 note), L-1 (PE integration MAPQ pin), L-2 (stray files). **HISAT2 backend COMPLETE: SE+PE, FastQ+FastA, all 3 library types, byte-identical to Perl v0.25.1 + HISAT2 2.2.2.** Reports: `CODE_REVIEW_{A,B}.md`, `COVERAGE.md`, `GATE_OXY.md`. Commit/PR on Felix's explicit ask.
- 2026-06-05: **Phase 2a IMPLEMENTED + GATED.** Full pipeline: implement (`/code-implementation`, 7 seams: config/aligner/options/discovery/error/report + naming in lib.rs+parallel.rs) → **dual `/code-reviewer` (A+B) both APPROVE, 0 Critical/High** + **`/plan-manager` COMPLETE** (271 tests: 228 lib + 43 integ; clippy -D + fmt clean) → **🎯 V9 SE oxy byte-identity gate PASSED**: `bismark_rs --hisat2` byte-identical to Perl v0.25.1 + HISAT2 2.2.2 single-core across SE {dir, non-dir, pbat} + FastA (10k; dir+non-dir also 1M: 844,267 / 843,765 rec). **🔴 GATE FINDING:** `--multicore`+`--hisat2` is NOT byte-identical (HISAT2's splice-site discovery is input-batch-global → chunking changes spliced alignments; Perl itself is not worker-invariant: 1310 vs 1219 spliced single-vs-mc8 at 1M) → **hard-rejected** in `config.rs` (Felix-approved). OQ-2d/2e/2f resolved (ambig single-core supported, multicore rejected, splice flags handled). Reports: `CODE_REVIEW_{A,B}.md`, `COVERAGE.md`, `GATE_OXY.md`. The PE read-1 `ZS` asymmetry remains 2b. Commit/PR on Felix's explicit ask.
- 2026-06-05: Phase 2 → drafted (rev 0) → manual-approved → **dual plan-review (A+B) → SPLIT into 2a + 2b.** Both reviewers verified the thin-wrapper thesis against the source but caught **two real byte-identity bugs** the plan would've shipped: (B-L1, the load-bearing find) the **PE read-1 `ZS` asymmetry** — Perl's PE read-1 parse loop (3372-3382) has *no* `ZS` branch so `second_best_1` is always undef→backfilled to `AS`, but the Rust uniform parser captures `ZS` for every record → wrong PE-HISAT2 MAPQ → non-identical BAM (SE-only spike couldn't catch it); and (B-L3) **`--dovetail` must be suppressed** for HISAT2 PE (Perl gates it `if($bowtie2)`; Rust pushes it unconditionally). Plus the **option order was wrong** (both: `--no-softclip --omit-sec-seq` appended *last*, not before the PE tail — Perl 8314), and three under-specified seams (A: `parallel.rs` 7th naming seam; index discovery is a 6→8 *suffix-arity* change; splice flags parsed-but-unhandled). SPEC §3.1 `--score-min` contradiction also fixed. **Split** (both reviewers' rec): 2a = detection+options(all deltas, append-to-finished-string keeps Bowtie2 structural)+discovery+naming(+parallel.rs)+SE gate; 2b = the PE `ZS` fix + PE/non-dir/pbat/FastA gate. **2a PLAN rev 1 written** (`phase2a-hisat2-core/PLAN.md`, all fixes folded), awaiting manual review → (then) dual plan-review of 2a or implement trigger. Reviews: `phase2a-hisat2-core/PLAN_REVIEW_{A,B}.md`.
- 2026-06-05: Phase 1 (HISAT2 determinism spike) → ✅ **Done — premise HOLDS.** On oxy (Perl v0.25.1 + HISAT2 2.2.2, 10k SE GRCh38): two `bismark --hisat2` runs **byte-identical** (8360 records, decompressed SAM @PG-filtered) → HISAT2 is run-to-run deterministic, byte-identity gate reachable (no concordance fallback). HISAT2 `aligner_options` = the Bowtie2 base (`-q --score-min L,0,-0.2 --ignore-quals`) **+ `--no-softclip --omit-sec-seq`**; same 2-instance directional strand model (`--norc`/`--nofw`); no seed/reorder/`-p` knobs. Final BAM tags = NM/MD/XM/XR/XG (no ZS/XS — ZS lives in the raw HISAT2 stream, parsed by `align.rs` for 2nd-best scoring; already `ZS`-or-`XS`-aware). 12/8360 spliced (N-CIGAR) records → the extraction N-op path is genuinely exercised (verify byte-equal in Phase 2). `SPIKE_hisat2_determinism.md`. Recommendation: proceed to Phase 2 (HISAT2 wrapper) at byte-identity.
- 2026-06-05: Epic created (`epic-writer`) — `bismark` aligner v1.x (HISAT2 + minimap2 wrappers), 5 phases, off `iron-chancellor` `fc38191` on `rust/aligner-v1x`. SPEC rev 0 approved by Felix (HISAT2 first; spike-first byte-identity; OQ1–5 leans accepted: one combined full-scale gate, directional first, human WGBS, oxy-only CI). Orientation established: HISAT2 = thin wrapper (same strand-instance model + `--norc`/`--nofw`, `ZS`-vs-`XS` already parsed, spliced-N already handled); minimap2 = wrapper + merge adaptation (no strand restriction → different ambiguity arithmetic, positional `.mmi`, `/1 /2` retention, `-ax sr`). All 3 bisulfite indexes present on oxy. Pins: HISAT2 2.2.2, minimap2 2.31-r1302. Phase 1 (HISAT2 determinism spike) kicking off next.
