Research harness for the FIFO streaming work. See `../STATE.md` for how to build and run.
Container-based; needs no local toolchain. `dev.sh` is the cargo wrapper, the rest is the
aligner validation matrix. Inputs are generated from the repo's own `test_files/`.
