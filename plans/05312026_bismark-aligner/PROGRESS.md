# Progress: Rust port of `bismark` (the aligner wrapper)

**Last updated:** 2026-06-04 (Phase 10 full-scale real-data gate PASSED — all 4 cells content byte-identical at full scale; dual code-review + plan-manager COMPLETE; docs bumped + fresh-branch PR opened for review. **Faithful aligner epic COMPLETE.**)

## Phase Status

| # | Phase | Status | Directory | Notes |
|---|-------|--------|-----------|-------|
| 0 | Determinism spike | ✅ Complete | `phase0-determinism-spike/` | Done 2026-06-01 — premise HOLDS; 2 gate refinements (decompressed-content gate; 2-line @PG). `SPIKE_determinism.md` |
| 1 | CLI + options + discovery | ✅ Complete | `phase1-cli-options-discovery/` | Done 2026-06-01. Coverage COMPLETE; dual code-review findings folded in (case-sensitive FASTA match lockstep w/ genome-prep, pbat/multicore validation, deferred-flag notice, README). 18 unit + 15 integration tests, clippy/fmt clean. Depends on #0 |
| 2 | Read conversion (FastQ SE directional) | ✅ Complete | `phase2-read-conversion/` | Done 2026-06-01. Coverage COMPLETE; dual code-review (both APPROVE) findings folded (deferred-flags, --mm2_maximum_length die, --prefix dot-trim, seqid_tab_count, double-uc). 36 unit + 15 integration tests, clippy/fmt clean. NOT yet committed. Depends on #1 |
| 3 | Single-instance align + SAM parse | ✅ Complete | `phase3-single-instance-align-parse/` | Done 2026-06-01. Coverage COMPLETE; dual code-review both APPROVE; +4 tests added. `align.rs` (SamRecord + AlignerStream peek/advance; not wired into run()). 53 unit + 15 integration tests, clippy/fmt clean. NOT yet committed. Depends on #1, #2 |
| 4 | N-way merge + scoring + MAPQ | ✅ Complete | `phase4-nway-merge-scoring/` | Done 2026-06-01. Coverage COMPLETE; dual code-review both APPROVE (calc_mapq verified Perl-vs-Rust bit-identical); +4 tests (full MAPQ-leaf pinning + 3075/3-instance/>4). `merge.rs`+`mapq.rs`, driver wired into run() (convert→2 instances→merge→counters, no BAM). 71 unit + 15 integration tests, clippy/fmt clean. NOT yet committed. Depends on #3 |
| 5 | Genomic-seq + XM/XR/XG + SAM/BAM (SE dir) 🎯 | ✅ **Complete + gated** | `phase5-genomic-seq-xm-sam-output/` | genome.rs+methylation.rs+output.rs+driver; dual code-review both APPROVE (B Perl-differential'd 27 deletion cases) + plan-manager COMPLETE; 111 unit + 17 integration, clippy -D + fmt clean. **§9 #18 oxy gate PASSED: byte-identical to Perl v0.25.1 + Bowtie2 2.5.5 at 10k (8,402 rec) + 1M (848,124 rec) reads (`GATE_OXY.md`).** Depends on #4 |
| 6 | Reports + ambig/unmapped (SE) 🎯 | ✅ **Complete + gated** | `phase6-reports-ambig-unmapped/` | report.rs + aux_out.rs + output.rs raw ambig-BAM + bismark-io `write_raw_record` (beta.9) + merge `Ambiguous{first_ambig}` + driver routing. Dual code-review both APPROVE + plan-manager COMPLETE; 131 unit + 19 integration + 179 io tests, clippy -D + fmt clean. **§7 #10 oxy gate ALL-PASS: BAM + report + --unmapped + --ambiguous + --ambig_bam byte-identical to Perl v0.25.1 (100k SE reads).** Depends on #5 |
| 7 | Paired-end support 🎯 | ✅ **Complete + gated** | `phase7-paired-end/` | PLAN.md rev 1; dual code-review (B caught the report-header Critical) + plan-manager COMPLETE; 193 tests green, clippy -D + fmt clean. **🎯 oxy PE gate PASSED**: byte-identical to Perl v0.25.1 + Bowtie2 2.5.5 at **10k + 1M pairs** (`_pe.bam` 1.7M rec + report + `_1`/`_2` unmapped/ambiguous + `_pe.ambig.bam`), identical argv + `--unmapped --ambiguous --ambig_bam` (`GATE_OXY.md`). Gate found + fixed a raw `--ambig_bam` RNEXT/PNEXT/TLEN drop. directional PE = 2 instances (slots 0/3). Full 10M+SE+RRBS = Phase 10. Depends on #5, #6 |
| 8 | Non-directional + pbat 🎯 | ✅ **Complete + gated** | `phase8-nondirectional-pbat/` | PLAN.md rev 1 + §12 notes; generalized `run_se`/`run_pe` + per-mode instance plans (`se_instance_plan`/`pe_instance_plan`) + conversion variants (`bisulfite_convert_fastq_se_ga`/`_pe_kind`) + 2 dispatch arms + per-mode temp cleanup. Dual code-review **both APPROVE** (both re-derived the GA-branch XM bytes from Perl) + plan-manager **COMPLETE**; **211 tests** (183 lib + 28 integration), clippy -D + fmt clean. 🔴 load-bearing closed: new `*BS_GA*`/G→A-reads-emitting fakes byte-assert FLAG/SEQ/XR/XG/XM on the first-live CTOT/CTOB (SE eff 2/3) + PE idx-1/2 paths. **🎯 oxy gate PASSED**: all 4 cells (SE/PE × non-dir/pbat) byte-identical to Perl v0.25.1 + Bowtie2 2.5.5 at **10k + 1M** (pe_nondir 1,703,244 rec; `GATE_OXY.md`). Directional SE+PE byte-frozen through the generalization. Depends on #7 |
| 9a | FastA input (SE+PE, all libraries) 🎯 | ✅ **Complete + gated** | `phase9a-fasta/` | PLAN.md rev 1 + §13; `convert_fasta_impl` (2-line core) + SE/PE FastA entries; lib.rs format-branch (dispatch/convert/re-read/`'I'×len` QUAL) + `write_fasta_record`; `options.rs` `-f` verify-only. Dual code-review **both APPROVE** + plan-manager **COMPLETE**; **226 tests** (194 lib + 32 integration), clippy -D + fmt clean. 🔴 FastA-aware fakes (`NR%2`/`^>`) byte-assert FLAG/SEQ/**QUAL=Phred40**/XM. **🎯 oxy FastA gate PASSED**: all 4 cells (SE/PE × {dir, non-dir}; pbat⊕-f dies) byte-identical to Perl v0.25.1 + Bowtie2 2.5.5 at **10k + 1M** (pe_dir 1,703,304 rec; `GATE_OXY.md`). FastQ byte-frozen. Depends on #8 |
| 9b | Order-preserving threading 🎯 (worker-invariance) | ✅ **Complete + gated** | `phase9b-threading/` | `config.multicore` field + `AuxWriter` enum + `process_*_chunk` extraction (N==1 byte-frozen) + new `parallel.rs` (2-pass split / `std::thread::scope` fan-out / ordered `merge_bams`+`merge_aux_gz` / `Counters::merge`). **Dual `/code-reviewer` both APPROVE + `/plan-manager` COMPLETE + round-2 delta re-review both APPROVE**; 240 tests (201 lib + 39 integration) green, clippy -D + fmt clean (run `--test-threads=2`). **🎯 oxy gate PASSED** (`GATE_OXY.md`): `--parallel 4`==`--parallel 1`==Perl single-core byte-identical, all 6 cells (SE/PE × {dir,non-dir,pbat}) at **N=10k AND N=1,000,003** real GRCh38 (pe_dir 1,703,348 rec) — proves the §2.6 Bowtie2-per-read-independence assumption. Remaining: commit/push/PR + re-base + `rm` 2 stray files (Felix's ask). Depends on #9a |
| 10 | Real-data gate on oxy 🎯 | ✅ **Complete + gated** | `phase10-realdata-gate-oxy/` | **🎯 oxy full-scale gate PASSED** — content byte-identical to Perl v0.25.1 + Bowtie2 2.5.5 across SE (84M/173 contigs) / PE (84M pairs/181) / mouse RRBS GRCm39 (46.7M pairs/52) / pbat; Gate A 10M strict+worker+measured-multicore-invariance, Gate B full content-multiset + V13 (pre-existing Perl `--parallel 4` BAMs same md5). Dual code-review + plan-manager COMPLETE; harness hardening folded (count backstops, `-S 16G`). `GATE_OXY.md`. **Faithful aligner epic COMPLETE.** Depends on #9b |

## History

- 2026-06-04: Phase 10 (full-scale real-data gate on oxy) → ✅ **Complete + gated. 🎯 oxy gate PASSED — faithful aligner epic COMPLETE.** Hybrid gate (PLAN rev 2): **Gate A** (10M subset, P=8) — Rust `--parallel 1` == Perl single-core byte-identical + worker-invariance (`--parallel 8`==`--parallel 1`) + the *measured* A1 assumption (Perl `--multicore`==single-core multiset) — all 4 cells (se/pe/rrbs/pbat) PASS; **Gate B** (full scale, P=8) — content-multiset (`LC_ALL=C sort`|`md5sum`) identical Perl `--parallel 8` vs Rust `--parallel 8` on 84M SE (71,325,696 recs / 173 contigs), 84M PE (143,434,086 / 181), mouse RRBS GRCm39 (55,387,646 / 52), pbat (143,434,062) + reports + count-reconciliation + distinct-RNAME-set + aux/`--ambig_bam` + perf; **V13** the pre-existing Perl `--parallel 4` SE+PE BAMs carry the SAME content md5 → four layouts (single-core / `--p4` / `--p8` / Rust) converge byte-for-byte at full scale. Felix-directed **pbat cell** (R1↔R2 swap → genuine CTOT/CTOB; closes O5 for pbat). Dual `/code-reviewer` (**both: no Critical, reported PASS trustworthy**) + `/plan-manager` (**COMPLETE, 0 gaps**, 2 documented DEVIATED) — convergent harness-hardening folded (Gate A record-count backstop + Gate B `PB`/`RB` emptiness guard + Gate A `-S 16G`; no re-run — results evidence-backed by captured md5s/counts). **Resource correction:** oxy advertises the node's 128c/991G but Felix's cgroup = **32c/256G** → gates run at **P=8** + absolute **`-S 16G`** (a `%` would size against 991G and risk OOM). DEVIATED (verdict-neutral): the B1.5 implied-count formula initially omitted the genomic-seq-extraction discard subtraction → false-flagged se/pe by exactly 36/74 → fixed (`(ubh−disc)×mult`) + re-verified + confirmed in situ on pbat; Gate A ran P=16 before the envelope was known, then Gate B at corrected P=8 (P-invariant correctness). Artifacts off-box on oxy `~/p10_artifacts/`. Docs bumped (`rust/README.md` row + Milestones + Next, EPIC, PROGRESS). Committed to a **fresh branch + PR opened for review (NOT merged** — Felix's standing rule). Stray test-output files `rust/bismark-aligner/reads_bismark_bt2.{bam,_SE_report.txt}` removed.
- 2026-06-03: Phase 9b → 📤 **Committed + PR open.** Re-based `rust/aligner-v1` onto `origin/rust/iron-chancellor` (`53033d0`, past #943/#944): `git reset --hard` is deny-ruled → used `git stash` → `git rebase --onto origin/rust/iron-chancellor 3272ecf` (empty replay range drops the redundant 9a commit, no `reset --hard`) → `git stash pop` (clean, no conflicts); re-verified 240 tests + clippy/fmt clean on the new base; diff vs base = exactly the 9b delta (incl. `+noodles-bam`, no version revert from #943/#944). Committed `5fa475b` (20 files; scoped to the aligner crate + Cargo.lock + `rust/README.md` status-journal row+Milestones+Next + EPIC/PROGRESS + the `phase9b-threading/` plan dir — session cruft excluded via explicit `git add`). Pushed fresh branch `rust/aligner-9b`; opened **PR #945** (`rust/aligner-9b` → `rust/iron-chancellor`, **MERGEABLE**; CI pending). Squash-merge on Felix's explicit ask. (Stray test-output files `reads_bismark_bt2.*` keep regenerating from a test writing to CWD — pre-existing hygiene nit, not committed.)
- 2026-06-03: Phase 9b → ✅ **Complete + gated. 🎯 oxy worker-invariance gate PASSED** (`GATE_OXY.md`). `bismark_rs` built on oxy (`dockyard-oxy-0`, cargo 1.96, noodles-bam 0.89 resolved) from the uncommitted `rust/aligner-v1` worktree; ran `phase9b_worker_invariance_gate.sh` (Perl single-core vs Rust `--parallel 1` vs `--parallel 4`) on real GRCh38, all 6 cells (SE/PE × {dir,non-dir,pbat}) at **N=10,000 AND N=1,000,003** (coprime to {2,4,8} → boundary straddled at every N): **byte-identical p1==p4==Perl** for the main BAM + `--ambig_bam` (decompressed SAM, `@PG` block filtered) + reports (wall-clock filtered) + `--unmapped`/`--ambiguous` aux (decompressed). pe_dir = 1,703,348 main + 103,110 ambig rec. Proves the §2.6 Bowtie2-per-read-independence assumption (the #1 risk). 🔴 **Gate-harness lesson:** the first run "failed" all cells — but the BAM diff was a SINGLE line (the `@PG CL:` argv, which embeds the per-run `-o`/`--temp_dir`/`--parallel`) with every record byte-identical, and the aux diff was RAW-gz-only (decompressed matched). Fixed the HARNESS (filter the whole `@PG` block — a worker-invariance gate can't use identical argv since `--parallel` IS the variable; gate aux on decompressed content — gz framing is an impl detail like BGZF for the BAM) + the local tests (`canon_bam`/aux → decompressed). **No production-code change.** Also surfaced: the raw-byte aux worker-invariance (orig §3.5/§9 #6 claim) does NOT hold at scale (inline-incremental vs bulk-merge deflate block boundaries) — only decompressed content does; §3.5/§9 #6 corrected. oxy scratch cleaned. **Remaining: commit/push/PR + re-base `rust/aligner-v1` onto iron-chancellor + `rm` 2 stray untracked test-output files — all on Felix's explicit ask.**
- 2026-06-03: Phase 9b → 🚧 **Round-2 delta re-review (fix-pass) — both APPROVE, clean.** On Felix's request, the post-review fix-pass delta (parallel.rs panic-payload + the cli.rs test/`canon_bam` changes) got a focused dual re-review: `CODE_REVIEW_A2.md` + `CODE_REVIEW_B2.md` — **both APPROVE, no Critical/High/Medium**, both ran the suite (240 green, clippy/fmt clean). Confirmed: panic→Err loses no orphan-safety (Drop reaps before payload observed); downcast correct/exhaustive; `canon_bam` raw reader equivalent for the main BAM + required for the tagless ambig BAM; `worker_error_propagates_no_hang` can't false-pass. 3 convergent Low notes accepted as non-blocking (panic-branch inspection-only; PE `.ambig.bam` assertion vacuous — covered by SE `a` cells + oxy; 2 stray files to `rm`). Remaining = oxy gate (§9 #11) + commit (Felix's ask).
- 2026-06-03: Phase 9b → 🚧 **Dual-code-reviewed + plan-manager COMPLETE + fix-pass** (NOT committed). Dual `/code-reviewer` (fresh contexts, recommend-only): `CODE_REVIEW_A.md` APPROVE-w/-minor-follow-ups, `CODE_REVIEW_B.md` APPROVE-w/-recommendations — **both no Critical/High**, both re-derived the invariants from source (contiguous partition, single-writer ordered merges, 22-field `Counters::merge`, skip/upto via the config clone, N==1 byte-frozen, noodles-bam pin matches). `/plan-manager` `COVERAGE.md` verdict **COMPLETE** (§3/§5/§9 ledger all DONE; the 4 documented deviations marked DEVIATED-documented). Convergent Medium findings folded: (A-M3/B-M1) `--ambig_bam` cross-N assertion added to `run_se_parallel`/`run_pe_parallel` — 🔴 surfaced a SECOND instance of the tagless-record trap in the TEST helper (`canon_bam` used the validating `bismark_io::BamReader` → panicked on the ambig BAM; fixed to a raw `noodles_bam` reader); (B-M2) `worker_invariance_pe_non_directional` added; (A-M1/B-M3, §9 #10) `worker_error_propagates_no_hang` added; (B-L2) panic sites now surface the payload (mapped to a clean `Err`, documented deviation from §3.8 "re-panic"); test count corrected 227→**240**. **clippy -D + fmt clean; 240 tests (201 lib + 39 integration) green** (`--test-threads=2`: the worker-invariance tests spawn up to 8 chunks × 2–4 fake-bt2 each, so default parallelism can SIGKILL — not a correctness issue). Remaining: oxy gate (§9 #11, the §2.6 assumption proof) + `rm` 2 stray untracked test-output files (`reads_bismark_bt2.bam`/`_SE_report.txt`, present since session start) before commit.
- 2026-06-03: Phase 9b → 🚧 **Implemented + locally verified** (via `/code-implementation`; NOT committed). `config.rs` `RunConfig.multicore` field (deviation: the plan assumed it existed — `--multicore` was `Cli`-only) + dropped from `deferred_flags`. `lib.rs` `pipeline` branches on `config.multicore`; new `AuxWriter{Gz,Plain}` enum (Sinks/PeSinks/open_*/write_pe_aux); extracted `process_se_chunk`/`process_pe_chunk` (convert+spawn+drive_merge+finish), `run_se`/`run_pe` (N==1) delegate keeping report/sink code verbatim (Q2a → N==1 byte-frozen). `merge.rs` `Counters::merge` (22-field sum). New `parallel.rs`: 2-pass `split_contiguous`(+PE common-min lockstep) with the converter's `(skip,upto]` arithmetic + plain subsets named off the original basename (no `.gz`); `run_*_multicore` via `std::thread::scope` (genome/header/refid shared, **skip/upto cleared on a RunConfig clone**, memory warning, lowest-chunk-index error); `merge_bams` (raw `noodles_bam::io::Reader` record copy), `merge_aux_gz` (one `GzEncoder`). `Cargo.toml` +`noodles-bam=0.89.0`. 🔴 **Gate-found bug** (content-addressed fake earned its keep): `merge_bams` first used `bismark_io::BamReader::records()` which requires `XR`/`XG`/`XM` → failed on the tag-less raw `--ambig_bam` (`missing required Bismark tag: XR`); fixed to a raw `RecordBuf` reader. **clippy -D + fmt clean; 201 lib + 37 integration tests green** — 5 new worker-invariance tests (`--parallel {2,4,8}` == `--parallel 1` byte-for-byte: BAM decompressed records + report modulo wall-clock + aux RAW gz bytes) over SE dir/non-dir/pbat + empty-chunk-at-high-N + PE dir; the 32 existing integration tests stay byte-frozen. Gate script `phase9b_worker_invariance_gate.sh` written (Perl WITHOUT `--multicore`, A-Imp6). Deviations (PLAN §13): RunConfig field added; raw `noodles_bam` merge reader; gate in the phase dir; N==1 STDERR ordering shift (byte-invisible). **Pending: dual code-review + plan-manager + oxy gate (§9 #11).**
- 2026-06-03: Phase 9b → 📋 **Plan ready (rev 1)** — dual plan-review complete (`PLAN_REVIEW_A.md` APPROVE-with-conditions, `PLAN_REVIEW_B.md` sound/implementation-ready; **both no Critical, no contradictions**; both source-verified every load-bearing claim — the three invariants, `Counters` sum-mergeability `merge.rs:63–122`, Perl stripe `:169`+offset-merge `:1458`, N==1-doesn't-subset `:308`/`:478`, `Drop` kill+reap `align.rs:255/461`). All findings = spec-tightenings/targeted-tests/gate-script-fixes, **zero design changes**, folded into rev 1: pin exact skip/upto arithmetic (converter's `(skip,upto]`, NOT Perl's subset counter) + both-set boundary test; aux raw-byte identity needs same-level+single-encoder+**no-mid-stream-flush** and is Rust-vs-Rust only (vs Perl=decompressed); confirm the 227-suite asserts full report body+aux before the N==1 delegation; **§9 #7 (fake)=machinery, §9 #11 (real oxy)=the Bowtie2-independence assumption — run #11 early**; **content-addressed** fake-bt2 + same-read→same-alignment + each decision class both-sides + empty chunk at high N (retires the false-pass trap); PE split over COMMON(min) count; orphan-safety already backstopped by Drop + lowest-chunk-index error; verify Bowtie2 exits 0 on empty input. A-unique: **oxy gate runs Perl WITHOUT `--multicore`** (strip `--parallel` from Perl argv); subset suffix tracks on-disk encoding (plain⇒no `.gz`); record-1 sanity per-chunk = error-path divergence; 226→227. B-unique: subset named off ORIGINAL basename, no prefix (+`--prefix`+N>1 test); memory warning "bounded by, not equal to". **Awaiting implement trigger.**
- 2026-06-03: Phase 9b (order-preserving threading) → 📝 **Planning** (PLAN.md rev 0 written; `phase9b-threading/PLAN.md`). Scope fixed by the kickoff (SPEC fork #4): `--multicore`/`--parallel N` = a `std::thread::scope` fan-out over **N contiguous** input chunks (NOT Perl's fork+modulo striping), each chunk running the full convert→2–4 Bowtie 2→lockstep-merge→per-chunk-BAM pipeline (reusing `run_se`/`run_pe`'s helpers), then an **order-preserving merge** — noodles record-copy BAM, one-`GzEncoder` aux, commutative `Counters` sum. 🎯 Gate = worker-invariance: `--parallel N` == `--parallel 1` == Perl single-core, byte-for-byte (NOT Perl's own `--multicore N` layout; NOT Bowtie 2 `-p`). `n==1` (default) keeps the proven direct path byte-frozen. Oriented on: `lib.rs` drivers + the `--multicore` seam (`cli.rs:149`/`config.rs:306,335`), the Perl fork/modulo/merge (61–118/169/1390–1482 — what we do NOT replicate), and the `bismark-extractor` #884 precedent (contiguous partition + ordered single-writer merge + commutative counters; **std-threads-not-rayon** deadlock lesson; `flate2`-not-`gzp` aux). #1 risk = the Bowtie 2 per-read-independence assumption (per-read content seeding → chunk-position-invariant), proven authoritatively by the oxy gate. Surfaced 5 Open structure/perf choices (chunk sizing 2-pass-exactly-N; N==1 delegates to a shared `process_chunk`; BAM record-copy; aux plain-temp+single-encoder; no concurrency cap below N) — none change output bytes. Re-base `rust/aligner-v1` onto iron-chancellor (drops redundant `3272ecf`) BEFORE the 9b PR — destructive, on Felix's explicit ask. Awaiting manual review → dual plan-review → implement trigger.
- 2026-06-02: Phase 9a (FastA input) → ✅ **Complete + gated**. Phase 9 split into 9a (FastA) + 9b (threading) per Felix. FastA = FastQ minus the quality line + `.fa` suffix + `-f` flag + synthesized Phred-40 QUAL (`'I'×len`, Perl 2707/3271); all strand/merge/FLAG/XM/report machinery reused (format-agnostic). Built on `rust/aligner` re-based onto `iron-chancellor 7f7d77d` (Phase 8 squashed into c865320). `convert.rs` `convert_fasta_impl` (2-line core, per-record `^>` sanity, PE gzip-off) + SE/PE FastA entries; `lib.rs` format-branched dispatch/conversion/re-read + `write_se_aux_record`; `aux_out.rs` `write_fasta_record`; `options.rs` `-f` verify-only. **Dual code-review both APPROVE** (Low only; both confirmed `convert_fastq_impl` byte-frozen + re-derived the FastA XM) + **plan-manager COMPLETE** (6 DONE/1 documented-deviation). **226 tests** (194 lib + 32 integration), clippy -D + fmt clean. 🔴 load-bearing CLOSED: the Phase-8 fakes hardcode `NR%4`/`^@` → false-pass on a 2-line `.fa`; new FastA-aware fakes use `NR%2`/`^>` + byte-assert **QUAL=Phred 40**. **🎯 oxy FastA gate PASSED** (`GATE_OXY.md`): 4 cells (SE/PE × {dir, non-dir}; pbat EXCLUDED — `--pbat ⊕ -f` dies, Perl 8155) byte-identical to Perl v0.25.1 at **10k + 1M** (se_dir 848,131 / se_nondir 847,444 / pe_dir 1,703,304 / pe_nondir 1,703,190 rec). Deviations (documented): separate `convert_fasta_impl` (not a shared `RecordShape`, keeps FastQ untouched); the planned pbat strand-test cell → non-dir (pbat⊕-f dies). Two test-bugs caught in impl: `.fa` reads in the genome dir get globbed as references (FASTQ-only convention) → reads in a separate dir; `--pbat -f` dies → non-dir cell. NOT committed → committed to rust/aligner this session (fresh PR off the re-based branch).
- 2026-06-02: Phase 8 → ✅ **Complete + gated**. Implemented via `/code-implementation`: `run_se_directional`→`run_se` / `run_pe_directional`→`run_pe`, each driven by a per-mode instance plan built at a single construction site (`se_instance_plan` `Vec<(orient,index,file)>` / `pe_instance_plan` `Vec<(slot,orient,index,k1,k2)>`); conversion variants `bisulfite_convert_fastq_se_ga` + the library-aware `bisulfite_convert_fastq_pe_kind` (directional fn delegates, byte-frozen); 2 dispatch arms `(SE/PE, FastQ)` for all 3 libraries; explicit per-mode temp cleanup (1/2/2/4 files). **Dual code-review both APPROVE** (A 1 Low doc nit fixed; B 3 Low nits — code correct; **both independently re-derived the GA-branch XM bytes from Perl** = `H.Z...` CTOB / `.z...H` CTOT) + **plan-manager COMPLETE** (all 7 items DONE). **211 tests** (183 lib + 28 integration), clippy -D + fmt clean. 🔴 load-bearing risk CLOSED: new GA-emitting fakes (`make_fake_bowtie2_ga_reads_{ct,ga}_index` SE + `..._pe_{ga,ct}_index` PE) map only the G→A reads on a chosen index → the first-live CTOT/CTOB (SE eff 2/3) + PE idx-1/2 paths run, and every test asserts `unique best: 1` + byte-asserts FLAG/SEQ/XR/XG/XM (cannot false-pass on all-unmapped). **🎯 oxy byte-identity gate PASSED** (`GATE_OXY.md`): `bismark_rs` built on oxy vs Perl v0.25.1 + Bowtie2 2.5.5 + samtools 1.23.1, real GRCh38, IDENTICAL argv into same `-o`, all **4 cells** (SE/PE × {non-dir, pbat}) byte-identical at **10k + 1M** (se_nondir 847,434 / se_pbat 4,645 / pe_nondir 1,703,244 / pe_pbat 3,182 rec; samtools @PG + wall-clock filtered). Caveat (Open Q3): directional datasets + mode flags land ~0 reads on the complementary strands → the integration tests, not the gate, prove the new strand arithmetic. Infra: oxy `/var/tmp` is ephemeral (wiped on restart) + SSH can drop/reschedule mid-run → use `setsid nohup` detached + frequent-poll keep-alive. oxy scratch cleaned. NOT committed → committed to rust/aligner this session.
- 2026-06-02: Phase 8 → 📋 **Plan ready (rev 1)** — dual plan-review complete (A 0C/4I/5O, B 0C/3I/4O; both APPROVE-WITH-FINDINGS, no contradictions, **no Criticals**). Both independently verified every §3.2/§3.3 slot/index/orient/file cell + all six "already-built" claims (incl. the pbat-SE `+2` modifier end-to-end). Findings folded into rev 1 — all coverage/precision, not wiring: 🔴 **test fakes must emit on `*BS_GA*`** + complementary strands (else non-dir/pbat tests false-pass on all-unmapped) + byte-assert the first-live GA/CTOT/CTOB/PE-idx-1-2 paths (the integration tests, not the directional-data oxy gate, are the load-bearing proof); PE conversion library-aware (don't overload the directional fn); per-mode temp cleanup an explicit task; run the directional regression guard before generalizing; `pe_gate.sh` nit. Open Q1 (generalize in place) + Q3 (reuse datasets) RESOLVED by Felix. **Awaiting implement trigger** (handoff + kickoff prompt prepared for a fresh session).
- 2026-06-02: Phase 8 → 📝 **Planning** (PLAN.md rev 0). SE+PE × {non-directional, pbat}, FastQ. Orientation (agent-surveyed Perl 5489–6025 conversion branches + 7096–7244/519–546/394–452 instance setup + 4308–4311 SE pbat `+2` modifier + 3112–3118/3851–3857 gated reject + 8146–8166 CLI). **Key finding: Phase 8 is mostly WIRING** — the CLI/config (`LibraryType` + conflict dies), the merge's `directional`-gated wrong-strand rejection, the SE genomic-extraction `+2` pbat index modifier (Phase 5), and the report's 3-way library line + gated rejected-count line (Phase-7 fix) are ALL already built (verify-only). New work = conversion variants (SE G→A/both; PE pbat-swap/both) + the driver's per-mode instance plan (generalize run_se_directional/run_pe_directional → run_se/run_pe). pbat changes only which instances spawn (+ SE `+2`), NOT FLAG/strand logic. Gate = reuse 10M_SE/10M_PE with `--non_directional`/`--pbat` (4 cells). Awaiting manual review → dual plan-review → implement trigger.
- 2026-06-02: Phase 7 → ✅ **Complete + gated**. **🎯 oxy PE byte-identity gate PASSED** (`GATE_OXY.md`): `bismark_rs` built on oxy from the worktree vs Perl v0.25.1 + Bowtie2 2.5.5 + samtools 1.23.1, real GRCh38 PE-directional WGBS (`10M_PE` subset), identical argv + `--unmapped --ambiguous --ambig_bam`. **10k PASS + 1M PASS** — `_pe.bam` (1,703,342 rec) + `_PE_report.txt` + `_1`/`_2` unmapped (355,576 ea) + ambiguous (237,736 ea) + `_pe.ambig.bam` (103,110 rec) ALL byte-identical (samtools `@PG` + wall-clock filtered). 🔴 **Gate found + fixed a real defect** the unit tests + dual code-review both missed: the raw `--ambig_bam` passthrough (`build_raw_record`, shared with SE) dropped the PE line's RNEXT/PNEXT/TLEN (fields 6/7/8) → rendered `* 0 0`; SE never exposed it (always `*/0/0`). Fixed + locked by a strengthened unit test; SE path unchanged. Full 10M PE + SE + RRBS = Phase 10. oxy scratch cleaned. NOT committed.
- 2026-06-02: Phase 7 → 🚧 **Implemented + reviewed**. Dual code-review (A APPROVE; **B REQUEST-CHANGES — 1 Critical**) + plan-manager (**COMPLETE**). 🔴 **C-1 (B) FIXED**: the PE report HEADER had the SE line order — Perl PE (1843/1846/1941) swaps the `was run with` and `--directional` lines + moves the `\n\n` to the last line (and the pbat wording differs, 1944 vs 1715); `write_report_header` now branches on `paired` via a `library_line(library, paired)` helper. §7 #25 validation gap CLOSED (`pe_ambig_lines_strip_read_tag_and_deconvert_rname_only`). Both reviewers confirmed the alignment/merge/extraction/SAM-output spine byte-faithful; SE paths untouched. **193 tests green (172 lib + 21 integration); clippy -D + fmt clean.** Reports CODE_REVIEW_A/B.md + COVERAGE.md. Only remaining gate = oxy PE byte-identity (Phase 10). NOT committed.
- 2026-06-02: Phase 7 → 🚧 **Implemented (code complete)**. All 8 modules built (convert PE converter, align PairedSamStream/PairedAlignerStream, merge check_results_paired_end + DecisionPaired + 4 PE counters, methylation extract_…_paired_end, output paired_end_sam_output + write_raw_pe_ambig_lines, report print_final_analysis_report_paired_ends + 2nd-file header + shared cytosine, aux_out mate param, lib run_pe_directional + PeSinks + drive_merge_pe). **192 tests green (171 lib + 21 integration); clippy -D warnings + cargo fmt --check clean.** All rev-1 findings addressed (incl. C-1 per-mate could-not-extract short-circuit). FLAG/TLEN unit-pinned to hand-derived Perl values; authoritative differential = oxy gate (Phase 10). Implementation notes in PLAN §12. Pending: dual code-review + plan-manager + oxy PE gate.
- 2026-06-02: Phase 7 → 🚧 **Implementing** (PLAN.md **rev 1**). Dual plan-review complete (A 0C/5I/6O, B 1C/5I/5O; both APPROVE-WITH-FINDINGS, **both independently re-derived all 8 high-risk claims from source → zero factual errors**; no contradictions, two severity splits resolved to the stricter rating). Folded: 🔴 Critical (B C-1/A I-2) per-mate could-not-extract contract (length guards in-order R1-short-circuit; non-failing mate keeps full seq; no pair-level flag); Important — TLEN total if/else-if partition, aux_filename extend-existing-sig + `mate`, pin FULL aligner_options (no `--minins` default), broaden FLAG/TLEN differential (equality + dovetail-gate-negative + `--no_dovetail`), scan-slot-vs-spawn-slot (slots 0/3, scan 0,3,1,2), `dovetail` param on paired_end_sam_output, +2-trim keyed on index not read_conv, two R1 id strings (@-stripped vs @-bearing), report strand-label join order 0,2,1,3 + ReportHeader 2nd file; Optionals (pos basis, ambig-BAM name, no-die, aux newline, TLEN basis, guard parametrisation, single-mate-XS test). Both endorsed parallel PE types (Q3) + the Perl-differential harness (Q6); both confirmed the §0 2-instance finding. Felix approved → implementing.
- 2026-06-02: Phase 7 → 📝 **Planning** (PLAN.md rev 0 written). Scope: PE-directional FastQ — PE read conversion (R1 C→T / R2 forward **G→A** + `/1/1`,`/2/2` IDs), **2** paired Bowtie2 instances (slots 0/3), `check_results_paired_end` (sum-of-AS selection, (77,141) no-align pair, directional reject index 1|2), per-mate genomic extraction (no fragment span), `paired_end_sam_output` (FLAG table + TLEN tree + mate-link fields), `_PE_report.txt`, `_1`/`_2` aux. 🔴 **Source-verified correction:** directional PE runs **2** instances, NOT 4 (kickoff + SPEC §1 wrong; "all 4" is non-directional only). `calc_mapq` already PE-ready (no change). Recommends parallel PE types (`DecisionPaired`/`BestAlignmentPaired`/`PairedSamStream`). Flagged `paired_end_SAM_output` (8713–9225) for direct re-read + a Perl-differential FLAG/TLEN harness (dominant byte-id risk). PR #930 confirmed all-green before start. Awaiting manual review → dual plan-review → implement trigger.
- 2026-06-02: Phases 1–6 committed + pushed to PR #930 (e850bea Phases 5–6 + e417716 merge-freshen of origin/rust/iron-chancellor). Conflicts resolved (workspace members union, README status table, Cargo.lock regen). **bismark-io kept at beta.8 (un-bumped from Phase-6 beta.9)** per the documented no-bump-during-beta convention surfaced by the merge (flagged to Felix). PR #930 MERGEABLE, title/body → Phases 1–6; **all CI green** (BismarkCI + cargo test + clippy + fmt + perl-oracle byte-identity). Does NOT merge to iron-chancellor without explicit ask.
- 2026-06-01: Phase 6 → ✅ **Complete + gated**. Dual code-review both APPROVE (no Critical/High; report bytes traced vs Perl 2004–2137) + plan-manager COMPLETE (all §3/§5/§7 DONE). Polish folded (QUAL-offset comment, crate-doc). **§7 #10 oxy gate ALL-PASS**: Perl v0.25.1 + Bowtie2 2.5.5 + samtools 1.23.1 vs Rust on 100k real GRCh38 SE reads (identical argv + --unmapped --ambiguous --ambig_bam) → BAM + `_SE_report.txt` (filter wall-clock + samtools @PG) + --unmapped.fq.gz (16,480 lines) + --ambiguous.fq.gz (44,988 lines) + --ambig_bam (10,069 records) ALL byte-identical. oxy workdir cleaned.
- 2026-06-01: Phase 6 → 🚧 Implementing (PLAN rev 2). Built: `bismark-io::BamWriter::write_raw_record` (beta.8→beta.9, +4 dependent pins); `merge` `Decision::Ambiguous{first_ambig}` (captured at both score arms, within-thread-only); `report.rs` (byte-exact report + wall-clock line); `aux_out.rs` (gz unmapped/ambiguous, un-stripped names, non-uc seq); `output.rs` raw ambig-BAM (`write_raw_sam_line_to_bam`); `config` `unmapped`/`ambiguous`/`ambig_bam` + deferred-flags shrink; `lib.rs` `Sinks` + routing + report + temp cleanup. 131 unit + 19 integration tests; clippy -D + fmt clean; workspace builds. Pending: dual code-review + plan-manager + oxy report-gate.
- 2026-06-01: Phase 6 dual plan-review complete (A+B independent; both APPROVE-with-findings, no contradictions). Caught 3 Criticals: (1) the SE report's trailing wall-clock `Bismark completed in …` line (Perl 926–927, REPORT never closed) → Rust must emit it + gate must normalize it; (2) `<genome_folder>` is absolute WITH a trailing `/` (7619–7629), canonicalize drops it; (3) ambig BAM written ONLY on within-thread ambiguity (2976), NOT cross-instance-tie (3091). Plus: Open-Q1(a) needs a new `bismark-io::BamWriter::write_raw_record` (cross-crate); `first_ambig` capture at 2806+2822; `(me+unme)>0` percentage gate; un-stripped unmapped/ambiguous filename; non-uc original seq. All folded into PLAN rev 2. Open Q5 surfaced for Felix (touch bismark-io vs defer --ambig_bam). Awaiting implement trigger.
- 2026-06-01: Phase 6 → 📝 Planning (PLAN.md rev 0). Scope: alignment report `_SE_report.txt` (byte-identical; embeds env-specific genome/read paths → needs identical argv like @PG) + `--unmapped`/`--ambiguous` gzipped FastQ (decompressed-content gate) + `--ambig_bam` (raw de-converted aligner SAM line passthrough). Routing precedence: ambiguous→ambiguous-else-unmapped; no-align→unmapped; rejected→drop. One Critical open Q surfaced: the `--ambig_bam` raw-record path bypasses `BismarkRecord` (Bowtie tags, not XM/XR/XG). Awaiting manual review → dual plan-review → implement trigger.
- 2026-06-01: Phase 5 → ✅ **Complete + gated**. §9 #18 oxy byte-identity gate PASSED — `bismark_rs` (built on oxy from the uncommitted worktree) vs Perl v0.25.1 + Bowtie2 2.5.5 + samtools 1.23.1 on real GRCh38 WGBS SE-directional reads: **10k → 8,402 records byte-identical** (incl. 1 chr-edge could-not-extract, counted-not-written) and **1M → 848,124 records byte-identical** (`diff <(samtools view -h …)` empty, samtools @PG filtered per P1; identical argv → Bismark @PG matches). Details in `GATE_OXY.md`. Full-scale SE+PE+RRBS gate is Phase 10.
- 2026-06-01: Phase 5 → ✅ Complete (local). Dual code-review both APPROVE (no Critical/High; Reviewer B diffed 27 deletion cases against live Perl `make_mismatch_string` → byte-identical) + plan-manager COMPLETE. Folded convergent findings: M1 (make_mismatch_string past-the-end → empty not NUL), +#16 (minus-strand+deletion double-revcomp, MD `3^G3`), +#13 (indels D-only + NM), +#14 (chr-edge → could-not-extract → header-only BAM). 111 unit + 17 integration tests; clippy -D + fmt clean. Real-data byte-identity gate on oxy (§9 #18) PENDING by design.
- 2026-06-01: Phase 5 → 🚧 Implementing (genome.rs + methylation.rs + output.rs + driver wired into run() writing BAM via bismark-io/noodles; @SQ from config.genome.fastas; phred64 RunConfig prereq; hard-reject --slam/--non_bs_mm/--rg_tag/--sam_no_hd; 108 unit + 16 integration tests green).
- 2026-06-01: Phase 5 → 🚧 Implementing (PLAN rev 1; dual plan-review both verified core logic vs Perl v0.25.1 — no Critical logic defects; all findings folded: @SQ from config.genome.fastas (one source of truth), phred64 RunConfig prereq, hard-reject --slam/--non_bs_mm/--rg_tag/--sam_no_hd, +2-not-in-MD-seq, indels=D-only, length-check-is-the-gate, minus+deletion double-revcomp, noodles @PG/@HD byte-diff validations, +5 validation rows).
- 2026-06-01: Phase 5 dual plan-review complete (A+B independent; both APPROVE-with-findings, no Critical *logic* defects; agreed on @SQ-order single-source + phred64 gap + multi-deletion test; folded into PLAN rev 1). Felix resolved §0 @PG → P1 (normalize-out).
- 2026-06-01: Phase 5 → 📝 Planning (PLAN.md rev 0 written; genome loader + genomic-seq extraction + XM/XR/XG call + SAM/BAM output via bismark-io noodles; first byte-identity gate). One Critical open decision surfaced for manual review: the samtools-pipe `@PG` policy (normalize-out vs reproduce best-effort).
- 2026-06-01: Phase 4 → ✅ Complete (coverage COMPLETE; dual code-review both APPROVE, calc_mapq Perl-vs-Rust differential-verified; +4 tests; 71 unit + 15 integration).
- 2026-06-01: Phase 4 → 🚧 Implementing (`merge.rs` + `mapq.rs` + driver wired into run(); 67 unit + 15 integration tests green, clippy/fmt clean).
- 2026-06-01: Phase 4 dual plan-review complete (A+B; both caught the SAME 2 CRITICAL — strand-counters belong to Phase 5; lockstep key needs `@`-strip); folded into PLAN rev 1 (+ exact 2nd-best conditional, flag==4 die, calc_mapq float verified bit-identical).
- 2026-06-01: Phase 4 → 📝 Planning (PLAN.md written; N-way lockstep merge + scoring + strand assignment + calc_mapq, SE directional).
- 2026-06-01: Phase 3 → ✅ Complete (coverage COMPLETE; dual code-review both APPROVE; +4 tests added; 53 unit + 15 integration).
- 2026-06-01: Phase 3 → 🚧 Implementing (`align.rs`: SamRecord + AlignerStream; 49 unit + 15 integration tests green, clippy/fmt clean).
- 2026-06-01: Phase 3 dual plan-review complete (A+B, both APPROVE/no Critical); findings folded into PLAN rev 1 (chomped raw_line, child kill+wait/drain-before-wait, tag-scan field-order/i64/short-line, +6 validations).
- 2026-06-01: Phase 3 → 📝 Planning (PLAN.md written; single-instance align + SAM-parse lockstep primitive).
- 2026-06-01: Phase 2 → ✅ Complete (coverage COMPLETE; dual code-review both APPROVE; fix-pass applied + tested; 36 unit + 15 integration).
- 2026-06-01: Phase 2 → 🚧 Implementing (`convert.rs`; 34 unit + 15 integration tests green, clippy/fmt clean).
- 2026-06-01: Phase 2 dual plan-review complete (A+B; B re-run after a session-limit cutoff); findings folded into PLAN rev 1 (--icpc Phase-1 mislabel, ReadProcessing seam, flate2 dep, synthetic golden, Perl loop order + skip-bypasses-sanity).
- 2026-06-01: Phase 2 → 📝 Planning (PLAN.md written; read conversion C→T FastQ SE directional).
- 2026-06-01: Phase 1 → ✅ Complete (coverage COMPLETE; dual code-review fix-pass applied + tested; 33 tests local / 34 Linux CI).
- 2026-06-01: Phase 1 → 🚧 Implementing (crate `bismark-aligner` implemented; 24 tests green, clippy/fmt clean).
- 2026-06-01: Phase 1 dual plan-review complete (A+B); 2 Critical `aligner_options`-order fixes + 5 other corrections folded into PLAN.md rev 1.
- 2026-06-01: Phase 1 → 📝 Planning (PLAN.md written).
- 2026-06-01: Phase 0 → ✅ Complete (determinism spike: premise HOLDS; gate refined to decompressed-SAM content + 2-line @PG policy pending).
- 2026-05-31: Epic created (SPEC rev 1 approved, all forks settled); all phases → 📋 Planned.
